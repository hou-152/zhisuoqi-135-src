# Phase 04 · rename-entry：一级导航更名「知识树」

**状态**：completed（2026-09-15 实跑验收后回填）

## 做了什么
- 顶栏按钮 `知识体系` → `知识树`；学习空间返回钮 `← 返回知识体系` → `← 返回知识树`；模板内其余用户可见「知识体系」字样一并改准（注释与内部 id 不动）。
- `data-view="graph"` / `setView('graph')` / hash 等**内部标识一律不改**（断言与链接不破）。

## 验收
- `grep -n "知识体系" prototype/知所栖-壳.html` 用户可见文案零残留（仅存于注释/数据引文时逐条核对）。
- 牵动断言改准：`test-daobi`（顶栏三格标签）· `check-public` · `test-app` · `test-path`（返回钮文案、默认模式）· `test-graph-view`。

## 取舍记录
- 更名放单独 phase：先立（树）后破（名），每步可独立验收；断言改准遵循 09-15 既有先例（`#rail`→`#topbar` 同款做法）。
