# 上下文腐烂 MVP 验收记录

日期：2026-09-13
页面：`http://127.0.0.1:5180/mvp-decision-context-rot.html`
实现：`prototype/mvp-decision-context-rot.html`
验收脚本：`scripts/test-mvp-learning.mjs`

## 黑盒结果

```text
✅ 0 条 JS 报错
✅ MVP 黑盒验收全过
```

脚本实际覆盖：

- 页面显示 QST、CON、OPI、CAS、SOL 五类材料 ID；主案例显示“假设场景”；
- 阅读按钮进入决策区，一题只显示 3 个选项；
- 错误选项停留当前题并显示反馈，不能绕过；选对后才能进入下一题；
- 三题全对才显示费曼区；
- 过短复述不通过；非 JSON 判定响应不通过；完整协议响应才显示通过；
- 没有向知乎发请求；通过后最后进度点才完成。

截图：`/tmp/mvp-decision-context-rot.png`。该截图是黑盒脚本在完成三题并注入一份协议完整的固定响应后取得的页面证据；它证明 UI 状态门，不代表模型效果样本。

## 本地模型冒烟

接口：`POST http://127.0.0.1:5180/api/llm`。`GET /api/health` 返回 `llm:true`；一次实际调用返回 `model: deepseek-flash` 和合法 JSON。给它一段漏掉控制变量的复述时，返回了以下漏点：

```text
固定模型、固定任务、固定关键证据、固定评分方法、只增加无关输入
```

这只证明本地接口、提示词和结构化返回协议可用。样本只有 1 次，不能据此宣称学习效果或模型准确率。

## 未覆盖

- 尚未把这个单章入口接到知识体系概念卡；当前先用独立页面跑通方法。
- 尚未装配 Agent Loop 其余五章；没有“下一章解锁”断言。
- 尚未验证公网静态版的人工复核页面；本 MVP 的本地模型路径只在本地服务存在时启用。
