# 知所栖 Demo v3 · 验证记录

日期：2026-09-06。当前文件：`prototype/知所栖-demo.html`。用户已确认产品名「知所栖」，同意沿固定任务、先检验、局部探索的方向推进；本轮检查不能替代真实用户试用。

## 交付与追溯

- 新入口为「知所栖」，页面标题、品牌、学习卡、讨论记录同步使用新名。
- `prototype/知枝-demo.html` 为兼容跳转，保留 URL 查询与 hash。同一浏览器源的断点仍使用原存储键。
- `prototype/archive/知枝-demo-v2.html` 保留修改前完整页面；已与 v2 验证哈希核对一致。v1、原文与历史证据保留原名。
- `docs/ai-video-map-v1.json` 仍为 18 个节点、27 条 soft 候选边；内容未变化。manifest 的内嵌模型位置已指向新入口。

## 浏览器实际操作

通过 CUA 浏览器接口在独立 QA 源 127.0.0.1:5179 操作，未刷新用户原有 5178 页面。DOM 用于检查结果，未直接写入学习状态。

| 观察 | 结果 |
|---|---|
| 首页读回已有 v2 断点 | 3 道已读且通过的概念保留，品牌显示知所栖 |
| 从演示工具重置本页后，进入先检验 | 直接进入景别题；不会新增读完记录 |
| 直接检验先答错，再答对 | 答错时无法继续；答对显示当前目标 1 / 3、未记为读完 |
| 未读答对后主动暂停，刷新恢复 | 个人记录仍是本题通过、未点读完、作答 2 次 |
| 我的记录中预览完成效果 | 假设记录为 3 项，每项均有一致的阅读、答案和作答次数；退出后仍是实际 1 / 3 |
| 混合完成先测与先读流程 | 景别、运镜未点读完且答对；提示词点读完且答对；概念题进度 3 / 3 |
| 完成组合应用 | 组合结果独立显示为通过；4 个练习标记，不额外增加概念题分母 |
| 学习卡导出面板 | 标题为知所栖；包含固定任务版本、3/3、独立组合结果，以及各概念真实的阅读和作答记录。本轮未重复执行文件下载 |
| 主题目录 | 全部 18 项按 6 项分为 3 页；实际切换到第 2 页仍显示 6 项 |
| 搜索不存在的主题 | 显示未收录提示；清空后恢复目录 |
| 分类筛选 | 画面设计仅显示对应的 3 张卡片 |
| 搜索并查看完成首支短片 | 只渲染当前目标和 3 个直接前置，共 4 个节点；未展开全部祖先；任务进度不变 |
| 普通叶节点视觉风格 | 提示当前候选地图没有后续关系，不误称已到作品目标 |
| 390 × 844 窄屏 | 文档宽度与视口均为 390，无页面横向溢出；局部关系按纵向呈现，当前概念优先。随后恢复默认视口 |
| 旧演示地址带 #map | 实际跳转到知所栖新地址并保留 #map；显示原先保存的 1 / 3 |
| 控制台 | 本轮读取的 error / warn 为空 |

默认桌面和窄屏均实际查看过渲染截图。

## 逻辑与数据检查

- 4 个内嵌脚本均可解析；`learning-model`、`atlas-model`、`focus-model` 与页面职责分开。
- 数据快照与内嵌节点／边一致，manifest 中的 SHA-256 匹配。
- 局部图每侧切片最多 3 项；当前 18 节点样本中最大的邻域实际为 6 个节点。目录每页 6 项，未靠透明度伪装过滤。
- 子代理通过一次性内存演算核对：v1／v2 正常暂停断点、v3 未读答对断点、三题及组合通过断点均恢复一致；复答出错撤销对应通过结果和组合结果，恢复后不复活标记。
- 只读审查发现并修复两处状态文案：预览样本通过但无作答次数、普通叶节点误称作品目标。修复后浏览器核对通过。
- 未新增测试框架或测试套件。

## 边界

这是缩小默认学习范围的可操作原型，尚未进行千级／万级性能实测，也未验证学习诊断准确性、先修关系有效性、长期掌握或用户接受度。课程仍是 3 个手写示例，未连接 AI、知乎或视频生成。

当前目录不是 Git 仓库，未创建提交、推送或部署。
