# 知枝 Demo v2 · 技能地图验证记录

日期：2026-09-06。对象：`prototype/知枝-demo.html`，地图数据版本「AI 视频入门地图 v1」。原 v1 的检查记录与哈希保留；原 v1 哈希现在对应 `prototype/archive/知枝-demo-v1.html`，已核对一致。

## 检查范围与方式

- 本地预览服务：127.0.0.1:5179，单独浏览器源用于 QA，未刷新用户原有的 5178 页面。
- 通过 CUA 的浏览器交互接口实际点击页面；DOM 用于读取页面结果，未通过页面脚本写入学习状态。
- 静态纯函数与 JSON 数据完整性检查；未新增测试框架或测试套件。
- 子代理对预览隔离、旧断点恢复、进度分母与文档做只读复核。最初发现的预览退出状态和损坏断点问题已修复并复查。

## 实际观察

| 操作 | 页面结果 |
|---|---|
| 初次地图加载 | 18 个节点；0 / 18，0%；3 个课程入口、15 个待制作 |
| 打开点亮效果预览 | 已读和练习通过均为 3 / 18，16.7%；4 个标记；明确显示预览提示 |
| 选择「完成首支 AI 短片」 | 显示 16 个候选前置节点；详情含拟观察表现、3 个直接前置及各自理由；课程待制作 |
| 退出效果预览 | 真实进度仍为 0 / 18，0% |
| 景别题先答错 | 下一步按钮不可用；阅读记录保留 |
| 景别题修正并「看它亮起来」 | 1 / 18，5.6%；景别节点点亮，1 个练习标记 |
| 预览期间从侧栏离开 | 首页显示真实 5.6%，没有模拟记录 |
| 更换为运镜问题 | 景别通过记录仍保留；后续完成运镜、提示词，共 3 个节点 |
| 暂停保存、刷新、继续 | 恢复 3 / 18，16.7%；继续到提示词已答对的位置 |
| 完成组合练习 | 4 个标记，地图仍是 3 / 18；组合题不另虚增节点 |
| 个人记录与导出面板 | 同一组 3 个概念；导出正文包含地图版本、3/18、16.7% 与真实作答次数；本轮检查导出面板及下载链接，未重复下载 |
| 再次答错景别 | 已读仍为 3 / 18；练习通过退为 2 / 18，11.1%；标记降为 2，组合结果撤回 |
| 预览 → 个人记录 → 技能地图 | 预览提示与点亮动画标志均清除；真实 2 个标记未改变 |
| 390 × 844 窄屏 | 页面宽度与文档宽度均为 390；图谱在自身区域滚动，页面无横向溢出 |
| 窄屏全景缩放 | 图谱内容宽度与容器均为 352；随后恢复默认桌面视口 |
| 浏览器控制台 | 本轮读取的 error / warn 为空 |

桌面与窄屏均实际看过渲染截图。图谱默认保留可读尺寸；「全景」同时按容器宽高适配，窄屏可放大后滚动查看。

## 数据与恢复校验

- 3 段内嵌 JavaScript 均可解析。
- 18 个 ID 唯一，27 条关系引用有效，图中未检测到环。
- 27 条边全部标注 `soft`、`candidate`，均有 `reason`；没有强制先修锁定。
- 数据快照与内嵌模型一致；manifest 包含节点、边、课程、分组计数与 SHA-256。
- 合法的 v1 三题断点恢复不丢记录；组合题空答案被丢弃；错答案即使带 `correct: true` 也重算为未通过；矛盾的反馈和通过记录被清理。

## 结论与未覆盖内容

本轮范围内的交互与数据检查通过。这不代表真实学习效果、课程正确性、地图先修有效性或用户接受度已得到验证。

地图、题目和理由是原创候选示例；未导入 Marble 数据或可视化代码，未连接 AI、知乎或视频生成。只有主动暂停才保存当前浏览器断点。18 节点包含不同颗粒度的概念与作品目标，覆盖率仅为这个版本的节点计数，不能解释为学习量或领域掌握比例。

目录不是 Git 仓库，没有创建提交、推送或部署。
