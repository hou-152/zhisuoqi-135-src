# 知枝 Demo 验证记录

日期：2026-09-06。范围：本地单文件原型的技术与交互检查，不是用户研究结果。

## 已实际检查

| 检查 | 观察结果 |
|---|---|
| JavaScript 语法 | 两个内联脚本均可解析 |
| 页面加载 | 本机 127.0.0.1:5178 正常加载，页面标题和入口完整 |
| 问题入口 | 三个推荐问题分别从景别、运镜、镜头提示词开始 |
| 概念示例 | 远景／近景／拉远起止画幅为可切换 SVG 示意；非生成视频 |
| 答错 | 景别题选远景后提示信息不足；阅读数为 1，练习通过数为 0；下一步禁用 |
| 修正后推进 | 景别题选车票特写后通过，继续到运镜；运镜与提示词题可继续完成 |
| 不允许跳过 | 演示工具中未阅读、未作答时尝试 NEXT，仍停留景别解释，阅读和通过记录均为空 |
| 实践案例 | 切换案例不会增加镜头提示词的阅读或通过记录；页面标注非真实知乎回答 |
| 组合练习 | 钥匙特写 → 拉远 → 空屋全景后，显示组合应用通过及实际作品验证建议 |
| 断点恢复 | 运镜解释处暂停，刷新同一 URL，恢复后景别通过状态仍在；组合练习的选择和结果也可保存恢复 |
| 首屏恢复一致性 | 修正为加载已保存快照后，首页知识树即可反映保存状态，不必先点继续才看见记录 |
| 学习卡下载 | 初版下载事件等待超时，但随后在 Downloads 中确认文件真实生成；读取正文与浏览器状态一致 |
| 导出改进 | 最终版先显示可复制全文，再提供 .md 下载链接，避免依赖下载事件；学习卡全文和讨论记录均已在页面回读 |
| 讨论记录 | 填入明确标注的技术检查样例后，导出文本正确包含该字段；未把样例当真实用户反馈 |
| 超范围提问 | 输入恋爱问题时，明确提示当前只有镜头课程，不假装回答；预设追问外的问题亦有边界提示 |
| 知识树 | 个人树仅列读过概念；总览显示 12 个示意节点，AI 视频对应当前状态，不宣称 AI 全领域占比 |
| 响应式 | 1280 px 桌面与 390 px 窄屏检查；窄屏 innerWidth = scrollWidth = 390，学习卡宽 354，无页面横向溢出 |
| 浏览器日志 | 主流程检查时未观察到页面 warning／error |
| 原始资料 | PDF 全 3 页文本读取，第 2 页渲染核对；初读时记录两份源文件的 SHA-256。收尾时微信临时 PDF 路径已不可访问，无法做结束哈希比对；文本提取与初始指纹已留存，实际复核状态见 source-final-check.json |

通过 CUA 的内置浏览器操作与界面回读执行上述检查，没有建立自动化测试套件。底层浏览器下载事件与实际落盘存在差异，故记录实际文件和全文回读结果，而不把事件超时写成产品失败或反过来。

## 仍未验证

- Safari、Chrome 等其他浏览器以及 file:// 场景下本地存储的差异。
- 真实知乎检索、内容引用、生成模型、视频生成、跨设备同步。
- 用户是否独立理解入口、是否迁移概念、是否愿意继续使用。
- 外部资料许可、团队是否接受候选方案、主办方是否认可范围。

## 交付状态

本次未对原始附件执行写入。本地目录原先为空且不是 Git 仓库，因此没有创建提交、切换分支、推送或部署。`Documents/PROJECTS-INDEX.md` 仅新增本项目入口登记。

代码指纹见同目录 `artifact-sha256.txt`。检查下载生成的学习卡保留为 `演示验证-学习卡.md`，仅为技术检查数据。
