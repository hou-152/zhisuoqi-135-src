---
id: cm_f8c2e9ae
name: 连接扇入
type: CONCEPTUAL
subject: AI 内参 260912
domain: harness-runtime
learningStage: when-needed
verification: judge
centrality: 0.089
depth: 1
origin: [neican]
aliases: ["Envoy / 雷霆之群"]
sources: 1
---

# 连接扇入

> Python 进程增多导致连接暴涨压垮下游；用 Envoy 升 HTTP/2、连接池与长连接收拢扇入。

**领域** harness-runtime ｜ **类型** CONCEPTUAL ｜ **什么时候学** 做到这里再懂 ｜ **怎么算会了** 能判 ｜ **中心度** 0.089

## 费曼一下

为了降低 asyncio 延迟，Habitat 增加了很多 Python 进程，但进程一多，连接数会暴涨，容易形成雷霆之群，压垮下游依赖或 NAT 网关。Envoy 把 HTTP/1 连接升级为 HTTP/2，利用多路复用、连接池和更长连接寿命，把相同请求合并到更少连接上，并集中实现限流和熔断。它保护下游，也支撑了 Python 服务的大规模进程模型。

## 原文 context

调整为降低 asyncio 延迟并拥有如此多的 Python 进程的一个副作用是，很容易使下游依赖项被大量的连接压垮（被称为“雷霆之群”）。

> We also rely on Envoy to maximize our connection fan-in. We use it to upgrade Python’s HTTP/1 connections to HTTP/2 to take advantage of multiplexing and then to pool those connections and extend connection lifetimes. Envoy also gives us a central place to implement rate limits and circuit breakers that would be less effective in each standalone Python process.

## 掌握证据（做到这些才算会）

- 能说出进程规模扩大容易形成“雷霆之群”，压垮下游依赖项
- 能说明 Envoy 把 HTTP/1 升级为 HTTP/2 以多路复用、池化并延长连接寿命，集中做限流与熔断

## 验收问句

> {{name}} 解决的是多进程模型带来的什么问题？

## 先懂这些（前置 1）

- [[亚稳态故障与 LIFOFIFO 连接重用]] · **soft** — 不懂 aiohttp 默认 LIFO 重用连接会把流量越推越集中在慢 Pod 上、压力退去后进程仍卡在降级状态，就做不好用连接池与长连接收拢扇入这件事。

## 出场

- AI 内参 260912 ｜ 《Rapidly scaling online storage to serve over 1 billion ChatGPT users（存储平台 Habitat 的扩容复盘）》 ｜ https://openai.com/index/scaling-storage-one-billion-users-part-one/

## 别名

`Envoy / 雷霆之群`

## 反链

- [[亚稳态故障与 LIFOFIFO 连接重用]]
