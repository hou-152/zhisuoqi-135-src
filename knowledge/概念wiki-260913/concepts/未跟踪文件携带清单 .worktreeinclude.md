---
id: cm_34e150d6
name: 未跟踪文件携带清单
nameEn: .worktreeinclude
type: REPRESENTATIONAL
subject: AI 内参 260912
domain: code-engineering
learningStage: when-needed
verification: use
centrality: 0.017
depth: 0
origin: [neican]
aliases: [".worktreeinclude"]
sources: 1
---

# 未跟踪文件携带清单 · .worktreeinclude

> 放在仓库根目录的清单文件，列出新建工作树时要复制的被忽略文件，用 .gitignore 式模式书写。

**领域** code-engineering ｜ **类型** REPRESENTATIONAL ｜ **什么时候学** 做到这里再懂 ｜ **怎么算会了** 能用 ｜ **中心度** 0.017

## 费曼一下

指在仓库根目录添加的一个专门配置文件，里面列出需要被自动复制进新工作树的被忽略文件（如 `.env`、`secrets.json` 等）。它解决了代码隔离导致必要环境变量丢失、进而导致后台测试无法运行的断档问题。

## 原文 context

If your repository ignores local setup files that a new worktree needs, add a .worktreeinclude file to the repository root and list the ignored paths or .gitignore-style patterns to copy when Codex creates a managed worktree.

## 掌握证据（做到这些才算会）

- 能指出该文件位于仓库根目录，内容是路径或 .gitignore 式模式
- 能解释它避免代码隔离后本地配置与环境变量丢失、后台测试跑不起来

## 验收问句

> 用 {{name}} 解决隔离后丢文件的问题，怎么写？

## 出场

- AI 内参 260912 ｜ 《Worktrees | ChatGPT Learn》 ｜ https://learn.chatgpt.com/docs/environments/git-worktrees

## 别名

`.worktreeinclude`
