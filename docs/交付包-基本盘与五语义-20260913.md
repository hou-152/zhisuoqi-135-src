# 交付包：AI 概念基本盘 ＋ 五语义内容工程（给队友）

生成：2026-09-13 00:5x（Asia/Shanghai）｜所有链接与命令**本机实测过**，实测结果写在每一条后面。

## 一句话

「AI 概念基本盘」（Context Engineering ＋ Harness Engineering 两份主题精选）已经拆成**五类语义单元**，
另有**三个公网地址**可以直接打开，和**三组可复制的命令**让你在自己机器上把它调起来。

## 一、交付包里有什么

| # | 是什么 | 在哪 | 怎么用 |
|---|---|---|---|
| 1 | **五语义内容单元库**：538 个单元（概念 76 · 方案 76 · 案例 76 · 观点 169 · 问题 141）＋ 7 张主题地图 ＋ 2 份选题装配稿 | `内容结构化系统/` | 读 `内容结构化系统/README.md` → 再读 `03-处理状态/处理状态总览.md` |
| 2 | **概念地图 v2**：918 概念 / 591 前置依赖 / 21 领域 ＋ 918 页概念 wiki（四源；AI 过滤两轮后；过滤前 1156） | `knowledge/概念地图-260913/`、`knowledge/概念wiki-260913/` | 直接读 JSON；wiki 丢进 Obsidian |
| 3 | **「Context × Harness 图鉴」站点**（基本盘的原始产物：76 概念 / 169 关系 / 40 张卡） | 公网双端 ＋ 本地 `/Users/housibo/Documents/ChatGPT/context+harness 网站/` | 见第二节 |
| 4 | **内参一栏**（当日期次文章，AI 内参图文格式） | 壳左栏第六格「内参」 | `node scripts/serve-135.mjs` → <http://127.0.0.1:5180/知所栖-壳.html> |

## 二、公网入口（三条，均实测 HTTP 200）

| 地址 | 是什么 | 实测 |
|---|---|---|
| <https://hou-152.github.io/zhisuoqi-135/> | 知所栖 135 公网 Demo（单文件零服务端） | `200` |
| <https://hou-152.github.io/context-harness-atlas-site/> | Context × Harness 图鉴（GitHub Pages） | `200` |
| <https://context-harness-atlas.sibohou792.chatgpt.site/> | 同一站点的另一路部署（OpenAI Sites） | `200` |

## 三、队友能直接复制的命令（都在本机跑过）

### 1）把 anthropic-art 这个配图 skill 装进你本机的各个 Agent

```sh
# ① 拿到 skill 源（公开仓库）
git clone --depth 1 https://github.com/HalfAI1102/anthropic-art.git /tmp/anthropic-art

# ② 一条命令装到本机所有已存在的 Agent（公共入口 ＋ Claude Code / WorkBuddy / Hermes / Kiro / Qwen / Grok）
bash ~/.agents/skills/dbs-install-skill/scripts/install-skill.sh link /tmp/anthropic-art

# ③ 看状态
bash ~/.agents/skills/dbs-install-skill/scripts/install-skill.sh status /tmp/anthropic-art
```

**实测结果（2026-09-13，本机）**：`link` 成功建立 6 个宿主入口（Claude Code / WorkBuddy / Hermes / Kiro / Qwen / Grok），
并**拒绝覆盖**已存在的真实目录 `~/.agents/skills/anthropic-art`（原样保留）——
这是这个工具的既定行为，不是失败。装完状态里 `✓ 未发现冗余入口` 才算干净。

> 本机现状：6 个宿主入口已指向稳定真源 `~/.agents/skills/anthropic-art`（不是 `/tmp`，临时克隆已删除）。

### 2）把「图鉴」的 agent 接口调起来（它是**只读**的）

```sh
cd "/Users/housibo/Documents/ChatGPT/context+harness 网站"
pnpm install
pnpm test:agent-interface     # 校验 llms.txt（76 行）＋ 本地只读 MCP 三个工具
pnpm mcp:graph                # 启动 stdio MCP server（不联网、不部署、不改数据）
```

**实测结果**：`LLMS_TXT_TEST_PASS concepts=76 bytes=20231` · `MCP_SERVER_TEST_PASS tools=3 positive=3 business_negative=2 protocol_negative=6 transport=stdio`。

### 3）把概念地图与五语义单元重新生成一遍

```sh
cd "/Users/housibo/Documents/知乎黑客松"
# 五语义单元（幂等，会先清空 02-内容单元库 下的 .md）
python3 内容结构化系统/07-脚本与工具/build-units.py
python3 内容结构化系统/07-脚本与工具/build-maps-and-assemblies.py
# 概念地图 v2（七步，两次 LLM 都带缓存，可断点续跑）
node scripts/cm-extract.mjs && node scripts/cm-merge.mjs && node scripts/cm-enrich.mjs \
  && node scripts/cm-edges.mjs --pass2 && node scripts/cm-build-map.mjs \
  && node scripts/cm-build-wiki.mjs && node scripts/cm-wire.mjs && node scripts/cm-validate.mjs
```

## 四、边界（别踩）

- **状态更新（2026-09-13）**：`zhisuoqi-135-src` **已公开** → <https://github.com/hou-152/zhisuoqi-135-src>。
  公开前做过全量凭证扫描并**重写了一次历史**（移除一条误入对话留档的知乎 access secret），对照见
  `docs/历史重写对照-20260913.md`。图鉴站仍**没有公开 remote**，解除它的 publication hold 仍需单独授权。
- **dbs skill 再分发这条已裁决**：2026-09-13 由所有者撤销（「DBS 的 Skill 本来就是公开的」），所以公开不再有内容障碍。
- 凭证一律不进产物：`.private/`（600，gitignored）、key 不打印、打包有自检（`scripts/build-app.mjs` 命中即失败）。
- 配图仍有一部分是**程序化 SVG**（本机没有位图生成工具），不是 Anthropic 位图；转位图的方法写在
  `docs/内参一栏-AI内参格式-20260912.md` 第四节。

## 五、下一步（按价值排序）

1. **76 个案例单元全是假设场景**（源卡 `scenario.type: hypothetical`）→ 决策场要真实案例，下一批从 58 篇正文里抽。
2. **去重候选 16,025 条失控** → 调 `generate-duplicate-candidates.js` 的阈值后重跑。
3. 把五语义单元接进壳（目前壳里是两条策展线「上下文工程」「Harness 与运行时」＋「内参」一栏，**五语义尚未单独成栏**）。

## 六、模块化：`ai-concept-base`（一个可安装的 skill 模块）

把 538 个单元打成一个**自带数据的 skill 模块**，拿走整个目录就能用（不依赖外部路径、不联网、不调用模型）：

```sh
# 装到本机所有已存在的 Agent（公共入口 ＋ Claude Code / WorkBuddy / Hermes / Kiro / Qwen / Grok）
bash ~/.agents/skills/dbs-install-skill/scripts/install-skill.sh link \
  "/Users/housibo/Documents/知乎黑客松/内容结构化系统/模块/ai-concept-base"
bash ~/.agents/skills/dbs-install-skill/scripts/install-skill.sh status ai-concept-base
```

**本机实测**：7 个入口全部建立，`status` 返回 `✓ 未发现冗余入口`；从已装入口跑查询正常
（`node scripts/query.mjs --q 护栏` → 3 条命中）。

模块里有什么：

| 文件 | 用途 |
|---|---|
| `SKILL.md` | 给 agent 的用法（装完自动可被调用） |
| `data/index.json` | 538 条轻量索引（先查它，别一上来读全文） |
| `data/units.json` | 538 个单元全文 |
| `data/lines.json` | 两条策展线（Context 34 / Harness 42 概念） |
| `scripts/query.mjs` | 按关键词／类型／主题检索，或看某条全文 |
| `scripts/assemble.mjs` | 把一个课题装配成「问题→概念→观点→案例→方案」草稿 |

不装也能直接用：`node 内容结构化系统/模块/ai-concept-base/scripts/query.mjs --q "上下文腐烂"`。

> **一条作废的阻塞**：原先写的「15 个 dbs skill 原文能不能再分发」未决事项，2026-09-13 由所有者撤销
> （「DBS 的 Skill 本来就是公开的」）。所以**公开不再有内容障碍**，只剩「把仓库转公开」这个动作本身需要一句授权。
