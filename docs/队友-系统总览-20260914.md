# 队友用 · 系统总览（2026-09-14）

> ## ⚠️ 文首必读：附件前提不符
>
> 任务书说这份 ZIP 里有一份「完整 Graph、语义与课程术语、复刻方法、关键结论、修改位置、验证方式」的 Markdown。**ZIP 里没有这份文档。**
>
> 实际成员只有 3 个（`unzip -l` 无隐藏项）：
> 1. `Issue2_复核裁决与四阶段验收_20260914.md`（17450 B）
> 2. `发给DeepSeek_继续收口Issue2.txt`（1627 B）
> 3. `manifest.json`（670 B）
>
> 包作者自述 `verification_scope`：「对用户所交文字回执的复核；**未执行用户仓库、模型调用、截图审查、发布或压测**。」
>
> **所以这份文件整理的是上面第 1、2 份的真实内容**（Issue 2 复核裁决 ＋ 四阶段验收路线），不是任务书描述的那份 Graph 文档。**Graph 的内容不在本包里**——仓库里真正的 Graph 材料是 `evidence/graph-260914/回执-全链路Graph-20260914.md` 与 `docs/总图视图契约-20260914.md`，要看那个请直接读仓库文件。
>
> 逐条核对见同批产物 `docs/后端与Skill接线-核对-20260914.md`。

---

## 0. 这份文件给谁看、怎么用

- **给队友**：想知道「负责人这一轮实际拿到的是什么、四阶段各自要什么、现在到哪了」。
- 每条都标了出处：`裁` = 复核裁决文件 `Issue2_复核裁决与四阶段验收_20260914.md`；`txt` = `发给DeepSeek_继续收口Issue2.txt`；`仓` = 仓库实际（本文件实测/逐字读码）。
- **三类内容显式分开**：① 负责人已明确要求 / ② 历史实现与已核对事实 / ③ 新增建议，未定案。
- **凡与仓库实际冲突的，以仓库实际为准，并当场标注。**

---

## ① 负责人已明确要求

> 判据：包内标为「已经确认」「不重开」「继续执行」的口径，或明确转述负责人裁决的条目。

| # | 要求（逐字引用） | 出处 | 仓库核对 |
|---|---|---|---|
| 1-1 | 「原 Issue 内的接线、解析、状态、测试与证据修复继续执行，不逐项问"继续吗"。」 | `裁:186` | — |
| 1-2 | 「负责人已经允许持续讨论并委托原范围内工程细节裁决。本文件不把该总目标扩张为账号、费用、发布或生产环境压力授权。」 | `裁:184` | — |
| 1-3 | 「发布、权限扩张、更换服务商、超预算、真实数据破坏性操作仍需明确授权。」 | `裁:189` | 与 `AGENTS.md` 硬边界一致 |
| 1-4 | 「真实调用遵守既有可核验预算；没有可核验的调用/费用上限时先完成离线部分并单列预算阻塞，不能自行假定免费或无限。」 | `裁:188` | 与仓库 `.private/llm.env` 凭证边界一致 |
| 1-5 | 「只做 agent-skills-api，不做技术实验台，不改图谱与路线，不碰 .private，不 push、不发布；真实调用沿用可核验的既有服务与预算。」 | `txt:13` | 实测：范围确为单篇试点 |
| 1-6 | 「不新增技术实验台；实验材料仅作为阅读和判断的参考。材料引用应与判据匹配，不为"换个东西"随机替换。」 | `裁:99` | graph 回执 `:166` 一致：「本轮只把材料与占位放进图（六章 6 个 `ExperimentReference` 标未启用），**没有新建技术实验台**」 |
| 1-7 | 「C2 v3 目标保持不变：解释实际加载与上下文占用的关系，区分文件存放和正文被加载；不要求背数字或实测。其他已经批准的学习目标不重开。」 | `裁:69` | **成立**：`evidence/feynman-teaching-map/agent-skills-api.json` 的 `criteriaVersion` 实测仍为 `v3-20260914` |
| 1-8 | 「决策题不是开始即时费曼的前置门槛。」 | `裁:96` | **成立**：`scripts/test-neican-inline-feynman.mjs` 与回执 `:82` 都验「读完阅读梯度就说（费曼在决策题之前）」 |
| 1-9 | 「即时费曼的 met 不替代决策作答，也不替代章末验收。」 | `裁:97` | **成立**：回执 `:173` 验「明说这不是正式掌握、也不解锁任何一章」；`:145`「即时费曼不解锁」 |
| 1-10 | 「任何回执只报告实际已经发生的结果，不承诺后台自动继续。」 | `裁:190` | 与 `AGENTS.md`「任何宣称必须有核实过的来源」一致 |
| 1-11 | 「每次一处补讲和至多一个追问；所有缺口可记录，但不一次性派发多项任务。」 | `裁:100` | **成立**：`serve-lib.mjs:201` 提示词要求 `"nextPrompt":"至多一个追问"`；回执 `:117`「追问只有一个（模型给了两行，只留第一行）」 |
| 1-12 | 「若需要一位开发者以外的试用者，先交可复现本地入口与说明，待负责人实际试用；不能把脚本学习者改称真人。」 | `裁:101` | graph 回执 `:156` 一致：「真人使用 ❌ **没有**｜零真实学习者记录。上面全部是脚本扮演的合成学习者」 |

---

## ② 历史实现与已核对事实

> 判据：包内标为「回执事实」或「复核判断」的条目——**描述已经发生的事**，不是要求。

### 2.1 当前阶段裁决

> 「**核心链路已有回执证据，单篇是本地 MVP 候选；Issue 2 尚不结项，也不直接进入全量自动装配。**」（`裁:23`）

| 层 | 包内引用的回执结论（逐字，`裁:27-32`） | 仓库核对结果 |
|---|---|---|
| A 确定性映射 | 「111 项、exit 0」 | **部分成立**：当时对；`node scripts/test-feynman-teaching-map.mjs` 实测现为 **117 项通过 / exit 0** |
| B 页面固定响应 | 「exit 0、0 条 JS 报错」 | **成立**（回执 `:452` PASS） |
| C 真实诊断 | 「解析 10/10、完整预期一致 4/10；另报 8/10 复测一致」 | **成立**：独立重算三个数字全部复现（详见核对文件 §2A-A4） |
| D 三轮真实反馈 | 「C3 说错 → 错误未纠正 → C3 讲对后焦点移至 C4」 | **成立**（回执 `:435`、`:437`） |
| 决策作答 | 「本轮未新答一题」 | **成立**（回执 `:459` 自述） |
| 公网/压测 | 「未发布；无压测证据」 | **成立**（graph 回执 `:212`、`:176`） |

### 2.2 证据边界（包自己划的，很重要）

> 「**"10/10 可解析"不是语义正确率；"4/10 整组完全一致"不是每个判据都错了六成；"8/10 复测一致"也不是准确率提升到 80%。** 三类量分别记录。」（`裁:85`）

> 「以下属于待查，不是已经证实的缺陷」（`裁:107`）

### 2.3 包内列出的「待查」项与仓库核对结论

| 待查项（逐字引用） | 出处 | 仓库核对结论 |
|---|---|---|
| 「`false` 可能是 `button.disabled === false` 的正确观测，不能把日志中的 false 一律判为失败。」 | `裁:108` | **成立**（提醒有效）。回执里大量 `｜ false` 确实是观测值，如 `:286`「第 2 章按钮解锁 ｜ false」——false 是**期望的观测**，不是失败 |
| 「六章测试既写"阅读后费曼已出现"，又写"三题后才出现"。核对是否检查不同模式/元素，以及即时与章末状态是否确实分离。」 | `裁:109` | **功能上分离（不是 bug）；但有一条断言写错了对象。** 详见核对文件 §2A-A7 |
| 「第六章被挡到第五章，不必然说明解锁安全；……不能把 URL 参数当权限控制。」 | `裁:110` | **设计如此的前置门**（`test-learn-agent-loop.mjs:220`、`:242`）。见核对文件 §2A-A8 |
| 「同一回执出现 918 与 999 个概念。先说明两者的集合、过滤口径或构建版本；没有口径时标数量待核。」 | `裁:111` | **已可结案**：918 是 `scripts/test-path.mjs:76` **断言名字里的过期标签**，不参与计算；**当前真实条数实测 999**（壳 payload 唯一 `cm_` id = 999，`manifest.json:27` `"topics": 999`）。见核对文件 §2A-A9 |

### 2.4 已知的真实缺口（照实记，不是待办承诺）

> 「**真实模型走查中发现并已修的一个真 bug**：提示词里的 JSON 占位示例写成 `"id":"F1"`，模型就照抄成 `F1`，而真实判据 ID 是 `agent-F1` → 全部落进"未知 ID → 未判定"。」（graph 回执 `:169`）

> 「**模型判断力不稳定（照实记）**：同一段复述，模型在第 2 轮判 `met`、第 3 轮又判 `partial`。这是**波动**，不是我把答案改成通过。」（graph 回执 `:171`）

---

## ③ 新增建议，未定案

> 判据：包内标为「执行要求」但**超出原 Issue 已确认范围**的，或标为「后续方案」的。**都还没有做，也不是已授权。**

### 3.1 R1–R5（原 Issue 内的收口工作，包称「执行要求」）

| # | 建议（逐字引用） | 出处 | 状态（仓库实测） |
|---|---|---|---|
| R1 | 「先冻结这次实际测过的版本与证据」 | `裁:38-52` | **未满足**。HEAD 已从 `aa9ac38` 前进 **13 个提交**到 `4dd73f0`；两份旧诊断 JSON **已从 HEAD 删除**（落在 `f8b5b2d`），**但可从 git 恢复**（`git show f8b5b2d^:…` 实测可读） |
| R2 | 「分开处理"接口非法"与"学习者没说清"」——「模型返回协议外状态、重复 ID、缺项……属于系统 `notJudged`，不能伪装成学习者表达含糊」 | `裁:60` | **未满足，且与代码现状方向相反**：解析失败／未知 ID／判据不全都已归 `notJudged`（`serve-lib.mjs:232/236/238`），但**协议外档位值仍降级为 `uncertain`**（`serve-lib.mjs:242`、壳 `:4576`），而现有测试 `test-neican-inline-feynman.mjs:204` 把「降级 uncertain」当**期望行为**在验。要改得同时改代码和那条断言 |
| R3 | 「C 层校准：修教学误判，不追逐漂亮百分比」；「不能未经核对继续把"缺少注入"当唯一原因」 | `裁:67-85` | **部分满足**：回执已提供注入断言；4/10 可复现。**建议本身（逐判据列预期/实际/教学影响）未做** |
| R4 | 「补上一次真正的场景判断，并从正常入口走通」 | `裁:87-103` | **未满足**（回执 `:459` 自述「未新答」） |
| R5 | 「已有日志歧义：核对断言含义，不制造假红点」 | `裁:105-113` | **部分完成**：918/999 与「第六章挡回」两项已由本次核对结案；费曼时机那条查出一条空断言（见核对文件） |

### 3.2 G2 / G3 / G4（包明确标为「后续方案」）

> 「这是后续方案，不是现在立刻批量生成。」（`裁:127`）
> 「发布和回退程序可执行，但实际 push/部署必须另有明确授权。」（`裁:152`）
> 「不必等十篇全部铺完。」（`裁:158`）

### 3.3 工具建议

> 「工具：优先复用已有工具；没有时可考虑 k6。k6 的 thresholds 才是自动通过/失败判定条件；仅写 checks 不会让整个测试因检查失败而退出失败。」（`裁:175`）

**仓库核对**：**仓库内查不到任何 k6 配置、脚本或压测产物**（`grep -rl "k6"` 仅命中 `app/package-lock.json` 的依赖字符串与一篇无关内参正文）。这是**外部工具建议**，与本仓库现状无关。

---

## 四阶段验收：逐阶段照抄 + 现状判定

> 「总目标：单篇 MVP 可用 → 分批铺开 → 完整流程可重复 → 在明确工作负载下通过后端压力验收。四项分别验收，不用一个"全绿"代替全部。」（`裁:19`）
>
> 下面每阶段的**目标／门槛／当前**三栏逐字照抄包内原文，再给**仓库现状判定**与命令。判定只用已跑通的证据，不含推测。

### G1：单篇 MVP 完成

| 项 | 包内原文（逐字） | 出处 |
|---|---|---|
| 要看到什么 | 「一个用户能够从现有入口独立开始，理解反馈针对什么，再次解释后反馈会更新，能作一次判断，并能恢复记录。」 | `裁:119` |
| 判定口径 | 「R1～R4 收口；关键错误处理和状态隔离通过；已知语义差异分级可解释；真实三轮证据与正常入口作答记录齐全。没有额外发布授权，交付仍为本地。」 | `裁:121` |
| 当前 | 「A/B/D 有回执支持，C 与完整作答证据未收口。不是"B/D 尚未做"，也不是"全量可用了"。」 | `裁:123` |

**仓库现状判定：🔴 未满足（部分满足）**

- ✅ B/D 有回执支持（`evidence/issue2-复核-20260914.md:452`、`:454`）。
- ✅ 「再次解释后反馈会更新」有证据：回执 `:167`「这一轮的重点换成了 C4」。
- ✅ 「能恢复记录」有证据：`test-neican-inline-feynman.mjs` ④ 节（刷新后草稿/轮次仍在）。
- 🔴 **R1 未收口**：冻结版本漂移（HEAD 前进 13 个提交，A 层 111→117）。
- 🔴 **R2 未收口**：非法档位值仍降级 `uncertain`（与复核要求相反）。
- 🔴 **R4 未收口**：「实际答一道决策题」自述未做。
- 🔴 **真人使用零记录**（graph 回执 `:156`），所以「一个用户能够……」这句目前只有**脚本扮演的合成学习者**证据。

```sh
# 可原地重跑的确定性证据（不需 serve、不调模型）
node scripts/test-feynman-teaching-map.mjs      # 实测：117 项通过 / exit 0
node scripts/check-learning-materials.mjs       # 回执记：344 项通过
node scripts/test-neican-inline-feynman.mjs     # 回执记：全过、0 JS 报错（需无头 Chrome）
```

### G2：分批铺开与完整内容目录

| 项 | 包内原文（逐字） | 出处 |
|---|---|---|
| 要看到什么 | 「推荐顺序：已通过的 1 篇 → 从现有候选里选少量不同结构的单元验证复用 → 经确认的十份五维资产清单 → 21 主题路线目录。实际数量、ID 和 hash 以接手时清单为准，不沿用口头估计。」 | `裁:129` |
| 每单元门槛 | 「每个新单元必须具备本单元的目标、判据、材料回指、反例/正例、决策依据和版本，不能把 C1～C4 模板强套到所有概念。」 | `裁:131` |
| 完成口径 | 「**目录全覆盖**：冻结目录每项有实际状态与缺口，不代表都可学。」／「**课程全量可学**：交付清单中的全部必选单元已 ready 且通过相应验收。仍 blocked 的必选单元不能靠"有缺口说明"算完成。」 | `裁:136-137` |
| 不许做什么 | 「ready/scaffold/blocked 继续保持清楚。没有材料就显示未装配，不伪造课程，不以节点总数充当学习完成率。」 | `裁:139` |

**仓库现状判定：🔴 未满足（规划中，已有施工单）**

- 现有可跑规模（graph 回执 `§4`）：**六章 ＋ 单篇 1 篇 ＋ 2 个标注为「开发夹具·不是正式课程」的单元**；「正式路线仍只有 1 条」。
- 仓库**已有一份施工单**（HEAD 提交 `4dd73f0`，只加了一个文件）：`docs/批量装配学习单元的施工单-20260914.md`（77 行）。**是施工单，不是执行结果**——按 G2 的口径，这属于「准备」，不属于「铺开」。
- **注意（2026-09-14 07:14 实测）**：施工单**已开始被执行**——工作区出现未提交的 `scripts/build-batch-units.mjs`、`scripts/check-batch-units.mjs`、`scripts/lib/batch-units-rules.mjs`、`evidence/batch-units-260914/units.json`（1.39 MB）。**这些不是本文件产出的**（本文件只写 `docs/`），**也未经本文件验证**。G2 的实际进度以那个会话的回执为准，本表不替它背书。
- 与 G2「不许做什么」一致：graph 回执 `:167` 明确「**全量课程可学：没有做到**。全量索引完成 ≠ 全量课程可学」。

```sh
node scripts/build-graph.mjs    # 生成/刷新全图索引（只读，不改公共源数据）
node scripts/check-graph.mjs    # 实测：通过 85 · 失败 0 / exit 0
```

### G3：完整流程可重复

| 项 | 包内原文（逐字） | 出处 |
|---|---|---|
| 学习侧 | 「选目标 → 阅读与即时反馈 → 判断 → 按既有规则完成章末验收 → 中断与恢复 → 返回路线或图谱 → 查看本次真实记录。」 | `裁:143` |
| 生产侧 | 「资料入库 → 来源定位 → 五维/语义装配 → 判据与案例核对 → 构建 → 确定性测试 → 有预算的真实模型检查 → 经授权发布 → 版本维护/回退。」 | `裁:145` |
| 关键要求 | 「不是每篇都靠改页面代码接入；数据缺项时明确阻塞，不落入通用默认标准。」 | `裁:148` |
| 关键要求 | 「旧学习记录绑定材料与标准版本；新版本不静默继承旧"已掌握"。不擅自删除用户记录。」 | `裁:149` |
| 关键要求 | 「生产/preview/固定响应测试分开，不伪造模型反馈或正式进度。」 | `裁:151` |
| 关键要求 | 「发布和回退程序可执行，但实际 push/部署必须另有明确授权。」 | `裁:152` |

**仓库现状判定：🟡 部分满足**

- ✅ **学习侧基本走通**：graph 回执 `:122` 记 `test-graph.mjs` ⑦b「一条会话从第 1 章阅读开始，逐章……走完六章 → 回看原问题和仍未知 → `choose` → 记录。全程 0 失败」。
- ✅ **中断与恢复**：graph 回执 `§6`「暂停记住原节点；跑开再恢复 → 回到**保存的那个节点**」。
- ✅ **「数据缺项时明确阻塞，不落入通用默认标准」已实现**：`serve-lib.mjs:293`「这个单元没有已审核的判据映射，不开正式判定（本轮试点只有 agent-skills-api）」（逐字）。
- ✅ **「不伪造模型反馈」已实现**：`serve-lib.mjs:313-319` 空正文/截断照实记 `not-judged`，不冒充通过。回执 `:206` 也自述固定响应脚本「不构成学习效果证据」。
- ✅ **回退程序可执行**：graph 回执 `§9` 给了两条 `git checkout` 命令。
- 🔴 **生产侧后半段未做**：`经授权发布 → 版本维护/回退` 实际未执行（graph 回执 `:212`「没有 push、没有发布公网、没有重新构建 `deploy/`」）。
- 🔴 **「不是每篇都靠改页面代码接入」未验证**：目前只有 1 篇单篇是真实接入的。

```sh
node scripts/check-learning-materials.mjs   # 材料体检（不需 serve、不调模型）
node scripts/paths-validate.mjs             # 路线配置体检
node scripts/build-graph.mjs                # 生产侧：确定性构建
```

### G4：后端压力测试与运行边界通过

| 项 | 包内原文（逐字） | 出处 |
|---|---|---|
| 判定口径 | 「这里的"通过"必须附带环境、版本、数据规模、请求行为、持续时长、限额和判定阈值。没有这些条件，不说"后端扛住了"。」 | `裁:156` |
| 现在就能做 | 「**现在就可以做的准备**：梳理实际接口、日志与关联 ID、请求/取消/超时链路、输入上限与预算护栏；不必等十篇全部铺完。最终容量验收在冻结的候选构建上执行。」 | `裁:158` |
| 两类环境 | 「1. 自有应用 + 受控模拟上游：测本服务的队列、限流、超时、取消、错误处理、请求隔离、资源释放和恢复。可在授权的本地或隔离环境做负载，不产生真实模型费用。」<br>「2. 自有应用 + 真实模型服务：只在已有或新增明确授权的服务、环境、并发和费用上限内做有限端到端容量验证。不能对供应商服务做无界施压。」 | `裁:161-162` |
| 三类量 | 「明确"同时在线人数""每秒提交数""同时在途模型请求"三种量。用学习者真实停顿建模，不假定三者相等。」 | `裁:166` |
| 不许做什么 | 「当前不存在的服务端功能标未实现，不造接口压测。」／「默认不自动运行付费负载。」／「凭证只使用既有安全配置，不复制到测试脚本或记录。」 | `裁:165`、`裁:173` |

**仓库现状判定：🔴 未满足（尚在准备阶段，零压测证据）**

- graph 回执 `:176` 逐字：「真实调用 3 次（走查）。另有修复前的 3 次。**没有跑循环、没有压测。**」
- **接口盘点（本次实测，来自 `scripts/serve-lib.mjs`）**——这是 G4「现在就可以做的准备」的第一项，可直接抄给队友：

| 方法 | 路径 | 会花钱 | 说明 | 行 |
|---|---|---|---|---|
| GET | `/api/health` | 否 | 返回 `{ok, llm, llmDisabled, llmRate, app, dataDir, version, packaged, root}` | `:252-259` |
| GET | `/api/skills` | 否 | `.agents/skills/` 清单 | `:260` |
| GET | `/api/graph` | 否 | 全链路 Graph 索引（`?summary=1` 只回统计） | `:265-276` |
| POST | `/api/search` | 否 | 知乎检索，spawn 项目内 CLI | `:278-281` |
| POST | `/api/learn` | **是** | 窄接口：单元 ID ＋ 复述 ＋ 输入版本 ＋ 请求 ID | `:284-324` |
| POST | `/api/llm` | **是** | 通用转发；可选 `skill` | `:326-342` |
| POST | `/api/orchestrate` | **是** | 多 Skill 编排 ＋ 知乎材料 | `:346-395` |
| POST | `/api/save` | 否 | **仅桌面版**（`DATA_DIR` 非空） | `:399-414` |
| POST | `/api/data` | 否 | **仅桌面版**：写「学习记录.json」 | `:417-424` |
| GET | 其余 | 否 | 静态页，`Cache-Control: no-store` | `:426-441` |

- **预算护栏（实测，`serve-lib.mjs:13-17`、`:69-90`）**：三个花钱接口共用一道闸——`LLM_DISABLED=1` → 503；`LLM_RATE_MAX`（默认 20）／`LLM_RATE_WINDOW_MS`（默认 60000）→ 429 带 `Retry-After`。**代码自述这是进程内计数**（`:65-68`）：「不是分布式配额，也不能冒充可靠的全局限额」。
- **输入上限（实测）**：`/api/learn` 复述 `LEARN_MAX_INPUT = 4000`（`:164`，`:296`）；请求体 64 KB（`:286`）；`/api/llm` 与 `/api/orchestrate` 请求体 4 MB（`:43`）；`/api/orchestrate` task ≤4000、prompt ≤20000、skills ≤3（`:348`、`:353`、`:362`）；`evidence` 每条截 300 字（`:242`）、`nextPrompt` 截 200 字（`:244`）；`/api/llm` 默认超时 30000 ms（`:143`）、`/api/learn` 25000 ms（`:308`）。
- **监听范围（实测）**：`scripts/serve-135.mjs:16` 硬编码 `host: '127.0.0.1'`，`serve-lib.mjs:55` 默认 `'127.0.0.1'`。graph 回执 `:174` 复核过「**没有改认证、没有改监听范围**（仍然只有 127.0.0.1）」。
- **未实现的服务端功能**（G4 要求「当前不存在的服务端功能标未实现」）：**没有**取消链路（客户端 abort 不会通知服务端停掉上游）、**没有**请求队列、**没有**持久化日志/关联 ID 存储（`console.log` 即走即丢，`:318`、`:322`）、**没有** `/api/learn` 的会话状态。

```sh
# G4 准备阶段可做（本机、零模型费用）——本轮均未执行
node scripts/serve-135.mjs &                    # 起服务
curl -s localhost:5180/api/health               # {"ok":true,"llm":…}
# 压测工具：仓库内无 k6 配置，需先选型（见 ③-3.3）
```

> **压测完成表述模板（照抄，别提前用）**，`裁:180`：
> 「在 [构建/配置/环境]、[单元规模]、[请求分布]、[负载与持续时长] 下，[延迟/有效结果/资源/成本/恢复] 满足预先冻结标准；上游为 [模拟/真实]；仍未覆盖 [范围]。」

### 四阶段判定汇总

| 阶段 | 判定 | 卡在哪（最关键一条） |
|---|---|---|
| G1 单篇 MVP 完成 | 🔴 未满足（部分满足） | R2：非法档位值仍降级 `uncertain`，与复核要求相反；R4 未实际作答 |
| G2 分批铺开与完整内容目录 | 🔴 未满足（规划中） | 正式路线仍只有 1 条；施工单已写、未执行 |
| G3 完整流程可重复 | 🟡 部分满足 | 学习侧走通；生产侧「经授权发布 → 版本维护/回退」未执行 |
| G4 后端压力测试与运行边界 | 🔴 未满足（准备阶段） | 零压测证据；且取消/队列/持久日志/会话状态四类服务端能力**未实现** |

**没有一阶段可以宣称通过。**

---

## 那份 `.txt` 到底在要什么

`发给DeepSeek_继续收口Issue2.txt`（17 行）是**给执行方的指令条**，不是分析。全文要点：

| # | 它在要什么（逐字） | 性质 |
|---|---|---|
| 0 | 「继续收口 Issue 2，参照《Issue2_复核裁决与四阶段验收_20260914.md》。」 | 指路 |
| 0 | 「本次复核已承认回执中 A/B/D 的进展，**不要把 B/D 重报成未做**；当前是本地 MVP 候选，不结项、不直接全量。」 | **纠偏**：防重复劳动 |
| 1 | 「冻结真正测过的工作树与证据版本，核对旧诊断 JSON 的删除/归档去向。**不得 reset 或提交他人并行改动**。」 | 执行要求 |
| 2 | 「非法状态属于系统未判定；只有合法 uncertain 才走学习者澄清。**补齐相应测试**。」 | 执行要求（**与代码现状冲突，见 ③-3.1 R2**） |
| 3 | 「将 C 层各次运行按判据/提示词/构建版本分开，逐判据列预期、实际和教学影响；区分格式成功、语义一致与重复运行稳定。**C2 目标不变，不为全绿改答案**。」 | 执行要求 |
| 4 | 「从正常入口完成真实连续反馈后，实际作答一道已有决策题，并验证返回/刷新恢复。提交**可打开的原始记录、截图和入口，而不只列路径**。」 | 执行要求 |
| 5 | 「日志里的 false 先核对是观测值还是断言失败；对读中/章末费曼、锁章回退、918/999 数量差异**只按实际实现核查，不猜测**。」 | 待查项 → **本次核对已结案 3 项**（见 ②-2.3） |
| 6 | 「原范围内的工程修复不逐项等待我回复继续。只做 agent-skills-api，不做技术实验台，不改图谱与路线，不碰 .private，不 push、不发布；真实调用沿用可核验的既有服务与预算。」 | **边界**（=①-1-5） |
| 7 | 「更大目标已纳入规划：单篇可用、分批铺开、完整流程、后端容量验收。当前可先整理后端接口与压测计划，**不运行未经批准的付费或生产压力测试**。」 | 规划 |
| 8 | 「下一份回执先给冻结版本、入口和实际作答证据，再给 A/B/C/D 与剩余阻塞。**不要只交更多绿灯数字**。」 | 回执格式要求 |

**一句话**：它在要一件很具体的事——**把版本冻住、把 R2/R4 真正做完、把 C 层按版本拆开逐条讲清，然后交可打开的证据，而不是更多通过数字。**

---

## 与核对结果冲突之处（以仓库实际为准）

| 包内说法 | 仓库实际 | 处理 |
|---|---|---|
| 「A 确定性映射 \| 111 项、exit 0」 | 实测 **117 项**（`node scripts/test-feynman-teaching-map.mjs`）。判据版本 `v3-20260914`、4 条判据均未变 | **以仓库为准**：引数字必须带提交号；`aa9ac38` 时是 111，`4dd73f0` 时是 117 |
| 「§1 的 HEAD 是 `aa9ac38`，工作区包含大量并行修改」 | HEAD 现为 `4dd73f0`（`main`），`aa9ac38` 是**祖先**、落后 13 个提交。工作区在本核对开始时（07:0x）只有 1 个未跟踪文件；**但 07:13–07:15 又被并行会话写入**（新增 `scripts/build-batch-units.mjs` 等 4 项，另有 5 张 png 被改） | **以仓库为准**：R1 担心的「混合工作区」风险**正在发生**，冻结版本请按「提交号 ＋ 文件 sha256」双轨记 |
| 「两份旧诊断 JSON 显示删除状态」（未说去向） | **已从 HEAD 删除**（落在 `f8b5b2d`），**但可从 git 恢复**：`git show f8b5b2d^:evidence/feynman-teaching-map/real-diagnosis-20260913200053.json`（25978 B，实测可解析，10 组） | **以仓库为准**：给得出可解析定位，不必标"缺失" |
| 「同一回执出现 918 与 999」 | **918 只是 `test-path.mjs:76` 断言名字里的过期标签**，不参与计算；当前真实条数 **999** | **以仓库为准**：已结案，见 ②-2.3 |
| 「页面测试 §2 写有『非法状态值降级为 uncertain』」 | 那句在 `test-neican-inline-feynman.mjs:201/204`；该文件小节编号是 ①–⑩，**仓库里没有「§2」** | **以仓库为准**：「§2」是复核文档自己的编号 |
| 「六章测试既写……又写……」 | **功能上两张卡确实分离**；但 `test-learn-agent-loop.mjs:148` 那条断言文案与所查元素错位，是空断言 | **以仓库为准**：是测试缺陷，不是功能 bug |

---

## 附：本文件未核实到的部分

1. **上游 dbskill / dbs-learning 的许可证**——未联网、未查上游，只核了本仓库（**仓库内查不到许可证文件**）。详见核对文件 §2B-B5。
2. **`test-graph.mjs`（210 项）、`test-graph-page.mjs`（40 项）、`test-learn-agent-loop.mjs`、`test-path.mjs`、`check-learning-materials.mjs`、`test-neican-inline-feynman.mjs` 本轮未复跑**（多需 serve 或无头 Chrome）。相关判定基于仓库内回执文件 + 静态读码，**不是本轮实测**。本轮实测的只有 `check-graph.mjs`（85/0）与 `test-feynman-teaching-map.mjs`（117/0）。
3. **`walk-graph-real.mjs` 真模型走查（11/0，3 次调用）本轮未复跑**——按硬边界禁止调模型。
4. **G4 的容量结论**：本轮**没有**做任何压测，上表只盘点接口与护栏，**不构成容量证据**。
