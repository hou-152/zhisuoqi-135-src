# 历史重写对照 · 2026-09-13

**为什么重写**：本仓库在转为公开之前做了一次全量凭证扫描，发现 `evidence/对话留档-知所栖135/` 的一份对话留档里，
**逐字粘着一条真实凭证**（知乎开放平台 access secret，40 字符）。它在当时的 `main` 与 `feat/neican-260912-daily` 上都能取到，
并被 1 个历史提交引入。

处理：用 `git filter-branch --index-filter` 把那一行替换成 `<ZHIHU_ACCESS_SECRET-removed-20260913>`（**只改了这 1 个文件、1 行**），
清掉 `refs/original/*` 与 reflog，`git gc --prune=now`，然后 force push 两个分支。

**验证（实测）**：
- 整段历史 blob 流里该凭证命中 **0 处**（`git cat-file --batch-all-objects` 全量扫）；
- 重写前后文件数一致（2716），`git diff --stat` 显示**只有那 1 个文件、1 增 1 删**；
- 公开后 `raw.githubusercontent.com/.../main/...` 取到的是**哨兵串**，凭证命中 0。

**残留风险（照实写）**：仓库里有一个**已合并**的 PR（#1），GitHub 会永久保留 `refs/pull/1/head`，它仍指向重写前的提交；
已实测该提交的 raw 内容**仍能匿名取到那串值**。PR 不能重开、ref 不能删 —— **唯一彻底的解法是去知乎后台轮换那枚 access secret**。
（另一条路：请 GitHub Support 清除该 ref，或删库重建。）

## 旧 → 新 SHA（只列最近 14 个提交，够解析文档里的引用）

| 重写前 | 重写后 |
|---|---|
| `c128a75` | `7906a7b` |
| `192da32` | `6056474` |
| `a7c3f41` | `8b4ec69` |
| `72af2af` | `d79bd5e` |
| `4c41403` | `cd776a8` |
| `2dbc001` | `3d76647` |
| `7a5d3e8` | `665f5a6` |
| `84ea507` | `0e6fb6c` |
| `29e6cd0` | `a0b7ea0` |
| `8e95614` | `1382718` |
| `e9c3b89` | `617244e` |
| `ded57f0` | `888631d` |
| `c48269b` | `ac8c9f4` |
| `b04b788` | `45c6570` |

> 更早的提交按同序一一对应，可用备份镜像 `/tmp/zsq-backup-1789277299.git`（临时目录，重启即清）重新生成完整对照。
> **`deploy/zhisuoqi-135/` 是另一个仓库，它的 SHA（如 `b5c5688`、`c78ea90`、`2b54a5c`）不受影响。**
