# 负责人已拍板方向（2026-09-14）· 施工依据

> **这份文件是裁决记录，不是建议。** 依据：`docs/总图与实践空间-方向研究-20260914.md` + `docs/标准答案-关系图谱与课程编排-20260914.md` 两份研究。
> 负责人原话见下，逐条已确认。**下游 agent 一律按这份执行，不要按研究稿里的其它方向做。**

## 最终方向（负责人原文）

```text
课程：先做 Agent Loop 六章阅读器
图谱：暂不排课，只做六章相关概念的局部审核和倒回候选
实践空间：已装配课程入口 + 材料状态看板
验证：负责人亲自走完一章，留下 1 条真实学习记录
扩展：六章体验成立后，再决定 19 条是否进入装配
```

## 六条决定（逐条）

### 1. 暂停「用图谱生成课程」——现在不做 D2
**理由不是边少，是两套 ID 没接上**：图谱是 `cm_*`，课程单元是 `CON-*` / `unit-*`，现有配对**只有 6 行**（`evidence/agent-loop-260913/pairings.json`）。所以「224 条可信 hard 前置」对课程单元的回补闭包**确实是空集**。
**图谱当前最多做三件事**：概念卡的前置倒回 · 人工诊断候选 · 以后审核台的数据来源。
**明确不参与**：排课、解锁、自动选材料。

### 2. 实践空间第一版 = 六章阅读器（**不是 6 + 19**）
六章已有：路线 / 正文 context / 决策题 / 费曼判据 / 来源链 / 已跑过的学习流程。
**19 条 ready 只是路线状态，不等于具备可读、可决策、可费曼的完整材料。**
第一版收窄为：`实践空间 → Agent Loop 六章 → 选择一章 → 阅读 → 形成性费曼 → 决策 → 章末费曼`。
**19 条只显示「准备中」或目录候选，不开放学习。**

### 3. 先做一条真实人工走查
**在继续扩展之前做。** 负责人本人完整走一章，留下：读了什么 / 哪一处没理解 / 费曼说了什么 / 系统指出了什么漏点 / 补讲是否有效 / 是否回到正确位置 / 决策题是否真的依赖前面的阅读 / 章末费曼是否像验收而不是再次聊天。
**不是学习效果实验，样本量 1**；作用是验证产品动作是否成立。

### 4. 图谱方向 = 轻量 D1 + 暂不 D2
不审 3000 多条边，也不直接用 224 条。先做**小范围人工审核台**，只处理：
`六章涉及的概念 + 它们的一阶前置 + 与这六章已有材料的配对`。
要回答的是：**这 6 个课程概念对应哪些 `cm_*`？哪些前置真的有教学材料？哪些边可以用于倒回？**
**如果这一步仍然没有稳定映射，就证明课程层和图谱层应继续分开。**

### 5. 三个代码问题先修（P0，不是方向争议）
1. `scripts/test-graph.mjs:92` **恒真断言** —— 守「不许把相关关系升级成先修」的测试是空的
2. `scripts/shell.template.html:1470` 页面声称关系线「已全部保留」，**实际 2485 条没有**（`relatedCount` 两处差 13 倍）
3. `scripts/shell.template.html:2999` `decisions.every(Boolean)` 对空数组返回 **true** —— **尤其这条必须先修**，否则没有决策题的批量单元会直接进入章末验收

### 6. D3 可以做，但只作为状态看板
缺口看板有现成数据、成本低，作为实践空间的**第二层**：
`已装配课程：可进入 / 材料缺口：待装配 / 缺少决策题：不可进入 / 缺少可靠案例：不可进入`。
**作用是讲清「为什么现在不能学」，不承担教学。**

## 执行顺序

1. **P0 三个代码修复**（决定 5）—— 先修，否则后面每一步都在错误行为上做
2. **实践空间 → 六章阅读器**（决定 2）—— 已知风险：会动 `test-learn-agent-loop` 的门控语义（72 项），改完必须跑全套回归
3. **状态看板**（决定 6）
4. **六章概念的局部审核台**（决定 4）—— 只碰六章 6 个概念 + 一阶前置 + 已有材料配对
5. **负责人亲自走一章**（决定 3）—— 在 1–3 做完之后
6. 六章体验成立后，再谈 19 条是否进入装配

## 硬边界（不因本裁决而放宽）

- 不改公共源数据（`knowledge/概念地图-260913/`、`units.json`、图鉴卡 yaml、`routes.json`）；个人状态不写回公共图谱。
- 不把「建议」写成「已实现」；不出现「学习效果已证明」。
- **不 push、不 commit**（负责人 2026-09-14 指示「后续再提交 GitHub」）。
- 76 个批量单元**保持不可学**：0 决策题、0 页面入口、264 条判据是机械反面转述 —— 页面必须照实标。

## 两条要一起记住的实测数字（来自研究，别再引用错）

- 图谱与课程**在同一张图里是两个不相连的连通块**：5725 条 `knowledge` 边与课程层、活动层接触数**各为 0**。
- 已审 hard 前置 **224** 条覆盖 313/999 = 31.3% 概念，**与六章 6 个概念、与 76 个批量单元的交集都是 0**。
- `check-learning-materials` 实测 **363** 项（AGENTS.md 写 344，已过期）；`test-path` 的 tab 断言是包含式比较，**加/撤 tab 不会假红**。
