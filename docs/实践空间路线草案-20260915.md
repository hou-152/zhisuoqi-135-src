# 实践空间路线草案（2026-09-15）· 待拍板

**状态**：草案，未接页面。负责人拍板顺序后才动 `shell.template.html`。
**数据**：`evidence/practice-route-260915/route-draft.json`（由 `scripts/draft-practice-route.mjs` 确定性生成，可复算）。
**边界（负责人 09-15 口径）**：知识体系＝浏览层，实践空间＝学习层，**两层不串联**。概念卡上现存的「学习这个 · 第 N 章」就是被否决的那种串联，接线这一轮一并撤掉——学习只从实践空间（和内参文章页）进。

## 结构：主线 + 七站

```
实践空间
├── 主线 · Agent Loop 六章（unit:chapter-*，人工策展，已可学，不动）
└── 七站 · 图鉴路线（70 个开放单元按站排队；6 个主线锚点位不重复学）
     [1] 眼前真正有什么     —— 模型此刻看见什么、装得下多少（8）
     [2] 信息平时放在哪里   —— 哪些信息被保留、进度记在哪（7）
     [3] AI 如何持续行动    —— 为什么能连续做事（14）
     [4] AI 如何接触外部世界 —— 靠什么读、算、改外部世界（8）
     [5] 人如何控制 AI      —— 怎么下目标、复用方法、限行动（14）
     [6] 信息如何进入工作台 —— 什么信息、何时、以什么形式进（15）
     [7] 做完后凭什么相信   —— 说做完之后凭什么信（10）
```

七问来自图鉴站方 `categories.yaml`，站序就是站方 order 1–7；「学完前面再学后面」的必修感由它承担（必修一 → 必修二 → …）。

## 站内顺序规则（可复算）

**主线锚点（superseded，置顶）→ now（打底）→ when-needed（用到再学）→ deep-dive（深挖）**。

- 主线锚点 = 与六章同概念的 6 个批量单元（方案丙封条），在路线里占位不重复学，点进去引导去六章对应章：`state-management`、`agent`、`agent-loop`、`agent-harness`、`tool`、`verification-loop`。
- now / when-needed / deep-dive 是图鉴卡自带的 `learning_stage`，站方策展信号，不是我发明的。
- **组内先后是课程组建议序，这就是要你拍板的部分**——直接改 JSON 里各站 `units` 数组顺序，或口头说哪站哪几步对调。

## 每站排序思路（一行一站）

1. **眼前真正有什么**：大语言模型 → 词元 → 窗口 → 注意力预算 → 上下文 → 无状态（先把「模型看得见什么」的事实打底），再工程侧（Token 底座、检索—推理双负担）。
2. **信息平时放在哪里**：记忆 → 文件工作区 → 仓库真源 → 隐性知识 → 可恢复压缩 → 代码图谱 → 状态管理（锚点）。
3. **AI 如何持续行动**：三个主线锚点（Agent / Agent Loop / Harness）打头 → Harness 工程 → CLI 运行时 → 生命周期 → 错误处理 → 交接 → Skill 链 → 子 Agent 编排 → 浏览循环 → 持久执行 → 事件驱动 → 提示词缓存。
4. **AI 如何接触外部世界**：工具（锚点）→ 工具—工作流适配 → 代码执行 → 行动空间 → 工具契约 → MCP → 工具收窄 → Schema 税。
5. **人如何控制 AI**：提示 → 提示词工程 → 系统提示 → 抽象高度 → 指令就近 → 信息引出 → 推理强度 → 终止条件 → 权限边界 → 沙箱 → 护栏 → 人类升级 → Skill → Harness 与计算分离。
6. **信息如何进入工作台**：上下文工程 → 选择 → 最小充分 → 动态装配 → 腐烂 → 迷失在中间 → 渐进披露 → 即时检索 → 观察掩码 → 压缩 → 限界上下文 → 聊天模板 → Skill 触发 → 会话管理 → 统一语言。
7. **做完后凭什么相信**：可验证目标 → 可运行证据 → 多步衰减 → 提示注入 → 验证循环（锚点）→ 可观测性 → 影响分析 → 风险分级自动修复 → 轨迹评测 → Harness 过拟合。

## 拍板后的接线（下一轮做，此轮不动）

1. 实践空间主区从「83 行看板」改成「主线 + 七站一条向前的路」：当前步高亮、走过的打勾、站头挂站方那一问；看板退到路线下方的「为什么有的还不能学」。
2. 撤掉概念卡的「学习这个」（知识体系回归纯浏览）；内参文章页的学习入口保留（内参 → 学习链路是你 09-15 自己要的）。
3. 顶部「继续学」接 `trajectories.json` 真实轨迹——同时是 264 条判据激活的唯一燃料。
4. 顺序配置独立成文件，改顺序不碰准入门逻辑、不碰已绿测试；新增断言：路线里 76 个单元与 units.json 一一对应、锚点位不重复开放。

**没做的**：没动页面、没动准入门、没动概念地图源数据、没 commit。草案生成器 76/76 全量校验（数量 / 归类 / 阶段 / 单元存在），两次运行除 `builtAt` 外逐字节一致。
