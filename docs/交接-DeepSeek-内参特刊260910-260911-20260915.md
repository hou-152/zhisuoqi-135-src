# 交接：内参主题特刊 260910（Context Engineering）/ 260911（Harness Engineering）

> ⚠️ **本文已作废（2026-09-15 晨）**：执行方从 DeepSeek Harness 换成 Antigravity（Gemini IDE），且现场已有部分产物（260910 三产物 12/28 · 260911 14/30，断点缓存有效）。现行版＝`docs/交接-Antigravity-内参特刊与概念提取v2-20260915.md`，本文只留档。

交接时间：2026-09-15 晨。交接方：ZCode 会话（概念网络图第三十三轮同一会话）。执行方：DeepSeek Harness。
一句话：**原料已切好落盘，你只管跑产线（三产物 → 页面数据 → 重建壳），跑完自检，ZCode 侧做验收微调。**

## 一、这是什么事

所有者拍板：内参日刊现有 260912–14 三期；**10 号、11 号做成两期主题特刊**——
- `260910`＝Context Engineering 特刊（Howie 原清单 26 篇 ＋ Codex 补充 2 篇）
- `260911`＝Harness Engineering 特刊（Howie 原清单 28 篇 ＋ Codex 补充 2 篇）

原料＝`evidence/概念源-260913/` 两份飞书主题精选·图文版（**含全部正文**，SHA256SUMS 冻结）。
月历与日报集合按期号排序，做完后 9 月月历应亮 **10/11/12/13/14 五天**。

## 二、已经做完的（不要重做）

- ✅ 切分器 `scripts/build-neican-special.mjs`（确定性、零 LLM；url／作者用图鉴站 sources.yaml 49 个来源逐字 probe 回填，对不上照实留空）。
- ✅ `knowledge/内参-260910/`：原文 28 份 ＋ issue.json（篇数 28 核对过；url 归属 25/28）。
- ✅ `knowledge/内参-260911/`：原文 30 份 ＋ issue.json（篇数 30 核对过；url 归属 28/30）。
- ✅ 三产物目录（三级笔记/概念辞典/AI费曼）已建空。**0 份产物**——此前一轮因模型问题被所有者叫停，没有遗留半成品。

## 三、你要跑的（按序）

**步骤 0 · 前置体检（必做）**：
```sh
node -e 'for (const l of require("fs").readFileSync(".private/llm.env","utf8").split("\n")){const m=l.match(/^\s*export\s+([A-Z_]+)=(.*)\s*$/); if(m&&!process.env[m[1]])process.env[m[1]]=m[2].replace(/^["\x27]|["\x27]$/g,"")}; const t=Date.now(); import("./scripts/lib/llm.mjs").then(async({chatCompletion})=>{const r=await chatCompletion({base:process.env.LLM_API_BASE,key:process.env.LLM_API_KEY,model:process.env.LLM_MODEL,messages:[{role:"user",content:"reply one word: pong"}],maxTokens:64,signal:AbortSignal.timeout(60000)}); console.log("LLM", process.env.LLM_MODEL, r.ok?"ok":"FAIL", ((Date.now()-t)/1000).toFixed(1)+"s", JSON.stringify(String(r.content||"").slice(0,40)))})'
```
- 默认模型＝deepseek-flash（llm.env，别改）。**flash 若超时／挂起：停下，报告所有者，等裁决。不许擅自换 v4-pro 或其他模型**——换模型是所有者的成本裁决，今晚已因此叫停过一轮。
- 顺带 `curl -s localhost:5180/api/health`，serve 没跑就 `node scripts/serve-135.mjs`（验收要用；产线本身直连上游、不依赖 serve）。

**步骤 1 · 三产物（真 LLM，断点续跑：产物 >200 字节即缓存，断了重跑不重烧）**：
```sh
node scripts/build-neican-daily.mjs --issue 260910 --concurrency=3
node scripts/build-neican-daily.mjs --issue 260911 --concurrency=3
```
两期可并行（两个后台进程）。58 篇 × 3 产物，要跑一阵——挂后台轮询进度，**不要固定 sleep 判完成**。

**步骤 2 · 页面数据（真 LLM 元数据：配图 SVG／星级／概念卡，带缓存可重跑）**：
```sh
node scripts/build-neican.mjs --issue 260910
node scripts/build-neican.mjs --issue 260911
```

**步骤 3 · 重建壳**：
```sh
node scripts/build-shell.mjs
```

**步骤 4 · 自检（跑过再说话）**：
- `ls knowledge/内参-260910/三级笔记 | wc -l` ＝ 28（概念辞典／AI费曼同）；260911 ＝ 30；
- 两期 `内参-页面数据.json` 存在、articles 篇数对；失败篇在日报 md 里**照实记账**，不静默吞；
- 起serve 开 `http://127.0.0.1:5180/知所栖-壳.html`：月历 9 月亮 **5 天**（10–14）；点 10 号 → 日报卡「Context Engineering」28 篇；点开一篇看三级笔记/概念辞典/费曼三签有内容；
- 回归：`node scripts/test-daobi.mjs` · `node scripts/shot-shell.mjs` · `node scripts/test-concept-net-page.mjs`（都要 serve 在跑）。

**步骤 5 · 登记与回报**：
- 工作日志 `docs/工作日志-知所栖135.md` 追加**新一轮**（第三十四轮被本交接占编，你从「第三十五轮」起编）：起因／做了什么／数字（tokens、篇数、失败清单）／没做什么。
- 回报：篇数 · tokens · 失败清单 · 月历截图。

**可选步骤 6（默认不跑）**：给两期特刊补「概念网络图」＝`node scripts/build-concept-net.mjs --articles 260910` ＋ `--articles 260911`（约 230 次调用，规格见 docs/概念网络图-产出规格-20260915.md）。**所有者点头才跑**；不跑的话特刊「概念网络」签只显示概念卡列表，是预期行为，不算缺陷。

## 四、硬边界

1. **不许碰 `knowledge/内参-260915/`**——那是另一个工作室在做的第四期（现 10 篇原文、无页面数据；壳不会显示它，保持这样）。
2. **不 push、不跑 build-public**——公网产物与发布由 ZCode 侧验收后统一处理。
3. **不改 `.private/`、不改模型配置**（flash 挂起就停下报告，见步骤 0）。
4. 不删不改既有产物；切分器不要再跑 `--write`（已落盘，重跑会因目录已存在而拒绝——这是保护）。
5. 仓库铁律照旧（AGENTS.md）：改完必须跑对应验收；引用要有核实来源；不得把示例数据当实验结果。

## 五、验收方（ZCode）会查什么

三产物齐且非空 · 页面数据篇数与失败记账如实 · 月历 5 天 · 特刊主题/篇数显示正确 · 抽查三产物质量（逐字引用、不堆列表）· test-daobi／shot-shell／test-concept-net-page 全绿 · 工作日志登记齐。验收不过的微调单会发给你。
