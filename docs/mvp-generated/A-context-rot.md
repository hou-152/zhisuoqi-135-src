# 装配草稿：能不能讲清「上下文腐烂」

> 由 ai-concept-base 自动装配：只搬运真实单元，未新增任何内容。
> 引用对外时请回到 `source_documents` 指向的源文，不要引本库当一手证据。

## 一、问题（入口）
- **能不能讲清「上下文腐烂」**（`QST-context-rot`）

> 如果模型在 100K 输入里偶尔漏答，但把相同证据放到 10K 输入就稳定，下一步应先查模型容量，还是查输入增长带来的利用退化？

## 二、概念
- **上下文腐烂**（`CON-context-rot`）
- **上下文**（`CON-context`）
- **上下文窗口**（`CON-context-window`）

### 上下文腐烂

Chroma 用“上下文腐烂”概括一组受控观察：在任务难度不变时，仅增加输入长度，也会使受测模型的表现更不可靠，而且退化并不均匀。

### 上下文

上下文是模型在当前一次推理中实际能看到并用来生成结果的信息、状态与工具描述。

### 上下文窗口

上下文窗口是一次推理能承载和处理 Context 的有限边界；它不是 Context，也不是长期记忆。

## 三、观点（费曼验收的判据）
- **方法论上的关键动作是：把任务难度钉死，只让输入长度变化。既有长上下文评测的通病是让「文本更长」和…**（`OPI-CTX-03-03`）

> 方法论上的关键动作是：把任务难度钉死，只让输入长度变化。既有长上下文评测的通病是让「文本更长」和「题目更难」同时发生，因此无法归因。

## 四、案例（决策场用；注意 case_type）
- **上下文腐烂：一个具体场景**（`CAS-context-rot`）

> 先确认模型在短输入条件下能稳定完成同一项问答，再保持问题、关键证据和评分方法不变，只分档增加无关材料；若表现随长度下降，才有依据把输入长度列为退化变量。
> 来源依据：Chroma 在 18 个模型上完成 194,480 次调用，并以“固定任务难度、只改变输入长度”为主要控制思路。语义检索、带干扰项问答、LongMemEval 与重复词复制等实验共同显示：长度增加时，可靠性会以不同方式下降。报告观察的是行为结果；关于结构为何影响注意力的解释仍是推测，未被证明为统一机制。

## 五、方案（实验台用）
- **上下文腐烂：可执行动作路径**（`SOL-context-rot`）

1. 先证明模型在短输入条件下能完成任务，再固定任务难度、相关证据与评分方法，只改变输入长度。
2. 将无关内容、近似干扰项、材料结构与证据位置分开记录，避免把多个变量揉成一个结论。
3. 同时统计正确、幻觉、弃答与复制错误等结果；平均准确率可能掩盖不同失败姿态。
4. 减少低信号内容后，用真实任务复测；不要只凭 NIAH 成绩或窗口标称值判断长上下文可靠性。

## 六、自测（讲完答得出才算过）
- **能不能讲清「上下文腐烂」**（`QST-context-rot`）

> 如果模型在 100K 输入里偶尔漏答，但把相同证据放到 10K 输入就稳定，下一步应先查模型容量，还是查输入增长带来的利用退化？

## 七、来源与状态
- `QST-context-rot`｜已核对（源：图鉴站已审计产物）｜SRC-EXT-003
- `CON-context-rot`｜已核对（源：图鉴站已审计产物）｜SRC-EXT-003
- `CON-context`｜已核对（源：图鉴站已审计产物）｜SRC-EXT-003
- `CON-context-window`｜已核对（源：图鉴站已审计产物）｜SRC-EXT-003
- `OPI-CTX-03-03`｜待核对（确定性解析自源文「核心观点」小节）｜SRC-EXT-001
- `CAS-context-rot`｜已核对（源：图鉴站已审计产物）｜SRC-EXT-003
- `SOL-context-rot`｜已核对（源：图鉴站已审计产物）｜SRC-EXT-003
- `QST-context-rot`｜已核对（源：图鉴站已审计产物）｜SRC-EXT-003