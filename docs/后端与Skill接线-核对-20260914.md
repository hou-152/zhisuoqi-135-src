# 后端与 Skill 接线 · 逐条核对（2026-09-14）

> ## ⚠️ 文首必读：附件前提不符
>
> 本次任务书称 ZIP `知所栖_本轮复核与阶段路线_20260914.zip` 里有两份 Markdown——一份「完整 Graph、语义与课程术语、复刻方法、关键结论、修改位置、验证方式」，一份「现有接口核对、部署步骤、默认 Skill 接线、调用示例、短答会话和上线边界」。
>
> **这两份文档在 ZIP 里都不存在。** `unzip -l` 列出的全部成员只有 3 个（无隐藏项），见 §1。对全文 grep `SKILLS_DIR|readSkill|learnPrompt|127\.0\.0\.1|host|port|staticRoot|defaultPage|dbskill|dbs-learning|许可证|license|graph` **零命中**（唯一命中是单元名 `agent-skills-api`）。
>
> 因此本文件分两块：
> - **§2A** 核的是**附件里真实存在**的、关于本仓库的断言；
> - **§2B** 核的是**任务书点名、但来源不在本附件**的断言（照核，来源另行标注）。
>
> 出处一律写清，不把两者混成一份「文档说的」。

---

## 0. 核对边界

- 只读核对。**没有部署、没有上线、没有改监听范围、没有动凭证、没有调用任何模型、没有 push、没有 commit、没有跑压测。**
- 没有改动 `scripts/` 下任何运行代码，没有改动公共源数据。
- 所有数字要么是**实测**（跑确定性脚本或 `node -e` 重算），要么是**逐字读文件**；两者都在行内标明。凡属推断，写「推断」。
- 「仓库内查不到」只表示**在本仓库里检索不到**，不表示上游不存在。凡此类判定都写明了检索命令。

---

## 1. 附件实际清单（实测）

```
$ unzip -l 知所栖_本轮复核与阶段路线_20260914.zip
  Length      Date    Time    Name
    17450  09-13-2026 20:42   Issue2_复核裁决与四阶段验收_20260914.md
     1627  09-13-2026 20:42   发给DeepSeek_继续收口Issue2.txt
      670  09-13-2026 20:42   manifest.json
    19747                     3 files
```

| 成员 | 字节 | sha256（实测） |
|---|---|---|
| `Issue2_复核裁决与四阶段验收_20260914.md` | 17450 | `9591fd59947fd2ec83525187a884ac4f44e96e5308f968ff318a915457c3df67` |
| `发给DeepSeek_继续收口Issue2.txt` | 1627 | `9deabbba758ae0978c022fec2152e9366574b15626329a7fc088831336caf074` |
| `manifest.json` | 670 | `1202551173b7114e55a0610383f09f6661ceae4dd28c6b46fd1ea64835402112` |

**任务书说的「两份 Markdown」，实际是一份 `.md` 加一份 `.txt`。**

`manifest.json` 自述：

```json
"source_file": "/mnt/data/issue2-复核-20260914.md",
"source_sha256": "d17acc56a931d3cdf9d0dd9e6e315cc1b9b42ac5bd1256ae67cc6d311360c204",
"verification_scope": "对用户所交文字回执的复核；未执行用户仓库、模型调用、截图审查、发布或压测。"
```

**关键发现：这个被复核的回执本身就在仓库里，且逐字节一致**（实测）：

```
$ shasum -a 256 evidence/issue2-复核-20260914.md
d17acc56a931d3cdf9d0dd9e6e315cc1b9b42ac5bd1256ae67cc6d311360c204  evidence/issue2-复核-20260914.md
```

461 行 / 27573 B，已跟踪（最近一次改动 `f8b5b2d`）。**所以包里的每条「回执事实」都可以在仓库内原地复核**，本文件正是这么做的。

---

## 2A. 附件里真实存在的、关于本仓库的断言

> 格式：文档原话（逐字引用）｜仓库实际（文件:行 + 逐字片段）｜判定

### A1. 自述证据边界

| 项 | 内容 |
|---|---|
| 文档原话 | 「未读取本机仓库、原始模型 JSON、截图像素或测试脚本，未独立复跑命令；本文件是对所交回执的复核与下一步任务编排，不是代码验收通过证书。」（`Issue2_复核裁决与四阶段验收_20260914.md:9`） |
| 仓库实际 | 与 `manifest.json:16` 同口径：「未执行用户仓库、模型调用、截图审查、发布或压测。」 |
| 判定 | **成立（自述）**。这是本包**最重要的边界声明**：包作者从未打开过本仓库，包内所有「仓库事实」都是转述回执。 |

### A2. 回执输入文件的 SHA-256

| 项 | 内容 |
|---|---|
| 文档原话 | 「回执输入文件 SHA-256：`d17acc56a931d3cdf9d0dd9e6e315cc1b9b42ac5bd1256ae67cc6d311360c204`。」（`:11`） |
| 仓库实际 | 实测 `shasum -a 256 evidence/issue2-复核-20260914.md` = `d17acc56…`，**逐字节一致** |
| 判定 | **成立** |

### A3. A 层「111 项、exit 0」

| 项 | 内容 |
|---|---|
| 文档原话 | 「\| A 确定性映射 \| 111 项、exit 0 \| 回执支持映射断言通过；未读取实现独立确认 \|」（`:27`） |
| 仓库实际 | 回执原话在 `evidence/issue2-复核-20260914.md:59`「费曼教学映射验收：111 项通过」。**但当前 HEAD 实测为 117 项**：<br>`$ node scripts/test-feynman-teaching-map.mjs` → `费曼教学映射验收：117 项通过` / `--- exit: 0 ---`（2026-09-14 实测）<br>漂移来源实测：`git diff --stat aa9ac38 HEAD -- scripts/test-feynman-teaching-map.mjs evidence/feynman-teaching-map/agent-skills-api.json` → 两文件共 `47 insertions(+), 6 deletions(-)`，由 `f8b5b2d` 引入。`criteriaVersion` 仍是 `v3-20260914`、判据仍是 4 条（实测） |
| 判定 | **部分成立**——回执当时为真；**该数字已过期**，现在跑是 117。 |

### A4. C 层「解析 10/10、完整预期一致 4/10；另报 8/10 复测一致」

| 项 | 内容 |
|---|---|
| 文档原话 | 「\| C 真实诊断 \| 解析 10/10、完整预期一致 4/10；另报 8/10 复测一致 \| 格式可解析不等于语义合格；还需同版本逐判据核对 \|」（`:29`） |
| 仓库实际 | 回执原话在 `:443`「- 解析：**10/10 成功、0 例未判定**」、`:444`「- 与预期完全一致：**4/10**」、`:445`「- 子代理复测：同条件 8/10 一致、2 组波动 → 记为**波动**，不称漂移。」<br>**独立重算（不调模型）**：`real-diagnosis-20260913203201.json` = 10 组 / parsed 10/10 / missing 集合一致 **4/10**；`real-diagnosis-20260913203426.json` 同；两轮逐组比对 missing 集合 = **8/10**（差 B1、B6，与文件内 `sameConditionRunNote` 逐字一致） |
| 判定 | **成立**（三个数字都可确定性复现）。**附一处不成立**见 §4-①。 |

### A5. HEAD 与两份被删诊断 JSON

| 项 | 内容 |
|---|---|
| 文档原话 | 「**回执事实**：§1 的 HEAD 是 `aa9ac38`，工作区包含大量并行修改；两份旧诊断 JSON 显示删除状态，其中一份又被 §4.2 引作结果依据。」（`:40`） |
| 仓库实际 | ① `aa9ac38` **存在**，是 HEAD 的**祖先**，距 HEAD **13 个提交**：`git log -1 --format="%h %ad %s" aa9ac38` → `aa9ac3820aaba154aa6109226c4e8f8076431528 Mon Sep 14 04:01:01 2026 +0800 fix(feynman): C2 按负责人裁决改 v3…`；`git rev-list --count aa9ac38..HEAD` → `13`。<br>② 回执 `:35-36` 确实列出这两行删除：` D evidence/feynman-teaching-map/real-diagnosis-20260913195225.json` / ` D evidence/feynman-teaching-map/real-diagnosis-20260913200053.json`。<br>③ 回执 `:442` 确实引它作依据：「文件：`evidence/feynman-teaching-map/real-diagnosis-20260913200053.json`（我上一轮跑，v3 判据）」 |
| 判定 | **成立**（回执当时三条都对）。**但整条已过期**，且「工作区包含大量并行修改」现在不成立——见 §6。 |

### A6. 「页面测试写有『非法状态值降级为 uncertain』」

| 项 | 内容 |
|---|---|
| 文档原话 | 「**回执事实**：页面测试 §2 写有"非法状态值降级为 uncertain"。」（`:56`） |
| 仓库实际 | ① 那句话是回执 `:145` 的一行输出：`  · 非法状态值降级为 uncertain（不得通过） ｜ true`，来自 `scripts/test-neican-inline-feynman.mjs:201,204`：<br>`201: /* 非法状态值 → 降级 uncertain，只给澄清 */`<br>`204: check('非法状态值降级为 uncertain（不得通过）', await ex(`(()=>{const r=nfxState('agent-skills-api').rounds.slice(-1)[0];return r.passed===false && r.statuses.C3==='uncertain'})()`), 'true');`<br>② **服务端真的这么做**：`scripts/serve-lib.mjs:242`<br>`      return { id, status: LEARN_STATUSES.includes(s) ? s : 'uncertain', evidence: String(row.evidence || '').slice(0, 300) };`<br>③ **壳里还有第二层同样的兜底**：`prototype/知所栖-壳.html:4576`<br>`    statuses[id] = NFX_STATUSES.includes(s) ? s : 'uncertain';   // 状态值不认识 → uncertain（不是通过）`（对 `/api/learn` 响应的兜底在 `:4765-4766`）<br>④ 实测（把两段源码抽出在 node 里跑纯函数，无网络）：传 `status:'passed'` → serve-lib 得 `C3:uncertain`、壳得 `statuses.C3='uncertain'`，**两者都不是 `notJudged`**；缺 `status` 字段也 → `uncertain`。 |
| 判定 | **成立**（测试这么写了，服务端与壳两层代码也真的这么做）。这正是包在 `:60` 要求改掉的行为——属于**待改**，不是已修。<br>**但「§2」这个编号在仓库里查不到**：`test-neican-inline-feynman.mjs` 自带的小节编号是 ①–⑩（另有 ⓪／⑦b／⑨），全仓库 grep 不到「§2」。那是复核文档自己的编号，不是仓库的——引用时不要当成仓库结构。 |

### A7. 「六章测试既写『阅读后费曼已出现』，又写『三题后才出现』」

| 项 | 内容 |
|---|---|
| 文档原话 | 「- 六章测试既写"阅读后费曼已出现"，又写"三题后才出现"。核对是否检查不同模式/元素，以及即时与章末状态是否确实分离。」（`:109`） |
| 仓库实际 | **断言在，且不止两条——一共 5 条相关断言**（`scripts/test-learn-agent-loop.mjs`）：<br>`:92` `check('读完就能讲：即时费曼卡在阅读之后就出现', await ex(`document.getElementById('learn-said')!==null`), 'true');`<br>`:95` `check('三题没走完时章末验收还没出现（不构成必须先答）', await ex(`document.getElementById('learn-final-said')===null`), 'true');`<br>`:133` `check('费曼一下在阅读之后就出现（读完就讲，不必等三道题）', await ex(`document.getElementById('learn-said')!==null`), 'true');`<br>`:146` `check('费曼框全章只有一份（走完三题不重复渲染）', await ex(`document.querySelectorAll('#learn-wrap #learn-said').length`), 1);`<br>`:148` `check('三题全对后才出现费曼', await ex(`document.getElementById('learn-said')!==null`), 'true');`<br>`:149` `check('这时才出现「章末验收」这张卡，并写明它才是解锁判据', await ex(`(()=>{const e=document.getElementById('learn-fey');return document.getElementById('learn-final-said')!==null && !!e && e.innerText.includes('章末验收') && e.innerText.includes('通过才解锁')})()`), 'true');`<br>`:150` `check('两张费曼卡是两个独立的提交口（即时 / 章末）', await ex(`document.getElementById('learn-said')!==null && document.getElementById('learn-final-said')!==null && document.getElementById('lin-submit')!==document.getElementById('learn-submit')`), 'true');`<br>回执 `:248` 与 `:256` 两行**同时报 `true`**。<br>**壳侧确实是两张独立的卡、两份独立状态**：即时费曼 `#learn-said`（`prototype/知所栖-壳.html:5060-5064`，读完就出现，「不改正式掌握状态、也不解锁任何一章」）／章末验收 `#learn-final-said`（`:5236-5239`，未答完三题只渲染「三题按顺序全对后才会出现」占位）；键也不同：`zss135.learn.inline.v1` / `zss135.learn.v1`。 |
| 判定 | **功能层面：不矛盾，两张卡确实分离**——即时费曼与章末验收费曼是不同元素、不同键、不同提交口（`:150` 正是断言这点），`:95` 与 `:149` 才是真正验「时机」的两条。<br>**测试层面：复核的怀疑成立，位置比它猜的更具体**——文档问「是否检查不同模式/元素」，答案是**有人检查对了，但 `:148` 这条写错了对象**：它的标签说「三题全对后才出现费曼」，查的却是阅读阶段就已存在的 `#learn-said`，与 `:92`／`:133` **逐字同一个表达式**，因此是一条**恒真空断言**（真正该查的是 `#learn-final-said`，`:149` 已经在查）。<br>**这是真实的测试质量缺陷（断言文案与所查元素错位），不是功能 bug。** |

### A8. 「第六章被挡到第五章」

| 项 | 内容 |
|---|---|
| 文档原话 | 「- 第六章被挡到第五章，不必然说明解锁安全；从全新状态检查最终可操作章本身是否可用。review/preview 只能在隔离模式显示材料，不写正式进度，不能把 URL 参数当权限控制。」（`:110`） |
| 仓库实际 | `scripts/test-learn-agent-loop.mjs:220`：`check('第 6 章仍未解锁（要逐章过）', await ex(`chapterUnlocked(5)`), 'false');`<br>`scripts/test-learn-agent-loop.mjs:242-243`：`check('地址入口不绕过解锁：第 6 章未解锁 → 挡回它的上一章', await ex(`document.getElementById('learn').classList.contains('on') && learnCur`), 'harness');`（并断言页面含「还没解锁」）<br>壳代码 `prototype/知所栖-壳.html:4849`：`const chapterUnlocked = (i) => i === 0 || chapterCleared(LEARN[i - 1].chapterId);`；`:4877-4885` 挡回上一章并写明原因（`第 N 章还没解锁：先把第 N-1 章…的费曼复述讲通过`）。顺序据 `evidence/agent-loop-260913/chapters.json`：harness order=5、verification-loop order=6。<br>审核口 `prototype/知所栖-壳.html:4015-4019`：「只有显式带 review=1（审核／复现用）才走 force 直达，学生入口不再能绕过解锁」→ `openLearn(cid, { force: review, from: ch.cm.id })`。回执 `:301` 对应行：`· 只有 review=1（审核／复现用）才可直达该章 ｜ verification-loop` |
| 判定 | **成立**（现象真实）。**成因是设计如此**——测试把「被挡回上一章」当成**真值**来断言（`:220` 期望 `false`、`:242` 期望 `harness`），不是回归。文档那条「不必然说明解锁安全」的提醒仍然有效：这条只证明「被挡回」，不证明「已解锁的章都可用」。<br>（「线性门该不该存在」属产品判断，代码层面无 bug——这一句是**推断**。） |

### A9. 「同一回执出现 918 与 999 个概念」

| 项 | 内容 |
|---|---|
| 文档原话 | 「- 同一回执出现 918 与 999 个概念。先说明两者的集合、过滤口径或构建版本；没有依据时标数量待核，不把任何一个数字当全量基准。」（`:111`） |
| 仓库实际 | 两个数字确实同现于回执：`:334`「· 六个 conceptId 全部能在 918 个概念里解析 ｜ OK」vs `:392`「· 999 个概念 / 632 条依赖（与地图源数据一致） ｜ 999/632」。<br>**成因已定位（实测）**：`918` 只是**断言名字里写死的旧标签**，不参与计算——`scripts/test-path.mjs:76`<br>`check('六个 conceptId 全部能在 918 个概念里解析',`<br>`  await ex(`ROUTES[0].steps.every(s => byId.has(s.conceptId)) ? 'OK' : 'NO'`), 'OK');`<br>该断言只查 `byId.has(...)`，与条数无关。而 `:192` 用现读值：`check(`${MAP_NODES} 个概念 / ${MAP_EDGES} 条依赖（与地图源数据一致）`, ...)`，`MAP_NODES` 在 `:22` 由 `knowledge/概念地图-260913/topics.json` 现读。文件头 `:17-20` 写明这次改造的原因：「这一条原本写死 918/591，当天把内参 260913 期 81 个概念并进地图（918→999 / 591→632）后它就红了 —— 地图变大是**预期内的合法变更**，红的不该是它。」<br>**当前壳 payload 实测 = 999**：`node -e` 对 `prototype/知所栖-壳.html` 取 `"id":"cm_[0-9a-f]{8}"` 唯一值 → `unique cm_ ids: 999`；`knowledge/概念地图-260913/manifest.json:27` → `"topics": 999` |
| 判定 | **成立，且已可结案**：918 是**过期标签文本**（另见 `scripts/make-mock.mjs:38`、`scripts/test-delivery-smoke.mjs:30`、`scripts/test-delivery-stress.mjs:132` 三处同样的旧标签），999 是**当前真实条数**。两者不是两套口径，是同一套口径的新旧标签。 |

### A10. 公网与压测

| 项 | 内容 |
|---|---|
| 文档原话 | 「\| 公网/压测 \| 未发布；无压测证据 \| 均不宣称通过 \|」（`:32`） |
| 仓库实际 | `evidence/graph-260914/回执-全链路Graph-20260914.md:212`「**发布状态**：**只在本地**。没有 `git push`、没有发布公网、没有重新构建 `deploy/`。」；同文件 `:176`「真实调用 3 次（走查）。另有修复前的 3 次。**没有跑循环、没有压测。**」 |
| 判定 | **成立** |

### A11. B 层与 D 层

| 项 | 内容 |
|---|---|
| 文档原话 | 「\| B 页面固定响应 \| exit 0、0 条 JS 报错 \| 回执支持页面与状态保护；不证明语义判断 \|」（`:28`）；「\| D 三轮真实反馈 \| C3 说错 → 错误未纠正 → C3 讲对后焦点移至 C4 \| 回执支持该合成样例的反馈变化与负对照；不代表真人学习效果 \|」（`:30`） |
| 仓库实际 | 回执 `:452`「\| B 页面闭环（固定响应） \| **PASS** \| `test-neican-inline-feynman.mjs` 全过、0 JS 报错（exit 0）」；`:435`「三轮：`D1-C3讲错` → `D2-负对照-错误未纠正` → `D3-C3讲对`」、`:437`「D3 讲对后焦点离开 C3、落到 C4」。实测 `inline-feedback-20260913203338/211441/211701/211814.json` 四个文件各 3 轮、各 21 条 assertions |
| 判定 | **成立** |

### A12. 「本轮未新答一题」

| 项 | 内容 |
|---|---|
| 文档原话 | 「\| 决策作答 \| 本轮未新答一题 \| 原"读、讲、补、再讲、作判断"链尚缺最后一次实际作答记录 \|」（`:31`） |
| 仓库实际 | 回执 `:459`「3. **「至少答一题已有决策」未新答**：决策题是既有可折叠参考，即时费曼按设计不阻塞也不替代。」 |
| 判定 | **成立** |

### A13. k6

| 项 | 内容 |
|---|---|
| 文档原话 | 「工具：优先复用已有工具；没有时可考虑 k6。k6 的 thresholds 才是自动通过/失败判定条件；仅写 checks 不会让整个测试因检查失败而退出失败。」（`:175`） |
| 仓库实际 | 检索命令：`grep -rl "k6" --include=*.mjs --include=*.js --include=*.json --include=*.md .`（排除 `node_modules`）→ 仅 `app/package-lock.json`（依赖字符串巧合）与 `knowledge/内参-260913/原文/ai-apps-not-a-good-business.md`（无关正文）。`ls scripts/ \| grep -iE "stress\|load\|perf\|bench"` → 只有 `test-delivery-stress.mjs`（该脚本自述是**交付物规模**走查，读 918 个 wiki 文件，不是服务端负载测试）。 |
| 判定 | **仓库内查不到**——仓库里没有任何 k6 配置、脚本或压测产物。文档这句是对**外部工具**的说明，与本仓库现状无关，不构成对仓库的断言。 |

---

## 2B. 任务书点名、但**来源不在本附件**的断言

> 这 5 条在 ZIP 里检索不到（见文首 grep）。以下按「仓库事实」照核，**出处标注为任务书，不标注为本包**。

### B1. `/api/llm` 读 Skill；`/api/learn` 不读

| 项 | 内容 |
|---|---|
| 断言原话 | 「`/api/llm` 有读 Skill 的逻辑（`scripts/serve-lib.mjs` 里 `SKILLS_DIR` / `readSkill` / `skill?` 参数），而 `/api/learn` 走自己的 `p.system`（判据 + 规则），**没有**调用那段 Skill 加载逻辑。」（任务书；来源不在本包） |
| 仓库实际 | `scripts/serve-lib.mjs:63`：`  const SKILLS_DIR = join(ROOT, '.agents', 'skills');`<br>`scripts/serve-lib.mjs:111-115`：`  function readSkill(name) {` / `    if (!/^[a-z0-9-]+$/i.test(String(name \|\| ''))) return null;   // 防目录穿越` / `    const f = join(SKILLS_DIR, String(name), 'SKILL.md');` / `    return existsSync(f) ? readFileSync(f, 'utf8') : null;` / `  }`<br>`scripts/serve-lib.mjs:330-335`（`/api/llm`）：`      if (body.skill) {` / `        const txt = readSkill(body.skill);` …<br>`scripts/serve-lib.mjs:302`（`/api/learn`）：`      const p = learnPrompt(found.map, source, said);`<br>**`/api/learn` 全分支 `:284-324` 内不出现 `readSkill` 或 `SKILLS_DIR`**（逐行核对）。全仓库出现位置收口：`readSkill` 只在 `:331`（`/api/llm`）与 `:357`（`/api/orchestrate`）；`SKILLS_DIR` 只在 `:63`（定义）、`:99`／`:101`（`listSkills`）、`:113`（`readSkill` 自身）。 |
| 判定 | **成立** |

### B2. 通用接口加载 Skill 时替换原有 system 消息

| 项 | 内容 |
|---|---|
| 断言原话 | 「通用接口加载 Skill 时**会替换原有的 system 消息**」（任务书；来源不在本包） |
| 仓库实际 | `scripts/serve-lib.mjs:333`（`/api/llm` 分支内，共 `:330-335` 五行）：<br>`        messages = [{ role: 'system', content: txt }, ...messages.filter(m => m.role !== 'system')];` |
| 判定 | **成立**。并且**比「替换」更强**：`filter(m => m.role !== 'system')` 是把调用方传来的**所有** system 消息**整条丢弃**，再把 SKILL.md **原文**（含 YAML frontmatter）作为**唯一** system 消息插到最前。原 system 消息不是被合并或改写，是不存在了。<br>**注意 `/api/orchestrate` 不是这个行为**：`:382-388` 把组合提示词与各 Skill 原文拼成**一整条** system，是追加式。两个接口的 Skill 装配语义不同。 |

### B3. `learnPrompt(...)` 只接当前复述

| 项 | 内容 |
|---|---|
| 断言原话 | 「现有 `learnPrompt(...)` 只接当前复述、不带会话与短答承接」（任务书；来源不在本包） |
| 仓库实际 | 定义在 `scripts/serve-lib.mjs:186`，**逐字**：<br>`  function learnPrompt(map, source, said) {`<br>三个形参：`map`（已审核判据映射）、`source`（源五维资产）、`said`（本次复述）。<br>装配出的 `user` 块 `:206-211` 只有四段，**逐字**：<br>`    const user = [` / `      `判据版本：${map.criteriaVersion}`,` / `      `判据与评分规则（每一条都要判，一个都不能少）：\n${criteriaBlock}`,` / `      `材料：\n${material}`,` / `      `学习者复述：\n${said}`,` / `    ].join('\n\n');`<br>请求侧只有两条消息：`scripts/serve-lib.mjs:307`<br>`      const out = await llmForward([{ role: 'system', content: p.system }, { role: 'user', content: p.user }],`<br>入口侧 `/api/learn` 只读四个字段，`:287-290`：`body.unit` / `body.said` / `body.inputVersion` / `body.requestId`——**没有会话 ID、没有历史轮次、没有上一轮 `nextPrompt`**。 |
| 判定 | **成立**。补充两点，避免误读：<br>① 提示词**要求模型输出**一个追问（`:201` `"nextPrompt":"至多一个追问"`），`:323` 也确实把它回给前端；但**下一次提交不会把它带回服务端**——每次 `/api/learn` 都是无状态的。所以「有追问」≠「有短答承接」。<br>② 前端 `prototype/知所栖-壳.html` 有本地轮次记录（`nfxState(...).rounds`，见 `scripts/test-neican-inline-feynman.mjs:204` 引用），但**只落盘不回流**，服务端看不到。 |

### B4. `host` / `port` / `staticRoot` / `defaultPage` 配置入口与默认监听

| 项 | 内容 |
|---|---|
| 断言原话 | 「服务是否已提供 `host` / `port` / `staticRoot` / `defaultPage` 配置入口、默认是否只监听 `127.0.0.1`」（任务书；来源不在本包） |
| 仓库实际 | `scripts/serve-lib.mjs:52-58`，**逐字**：<br>`export function createZssServer(opts = {}) {` / `  const ROOT = opts.root;` / `  const PORT = Number(opts.port \|\| 0);          // 0 = 让系统给一个空闲端口（桌面版用）` / `  const HOST = opts.host \|\| '127.0.0.1';` / `  const DATA_DIR = opts.dataDir \|\| null;        // 非空 = 开放 /api/save（桌面版）` / `  const STATIC_ROOT = join(ROOT, opts.staticRoot \|\| 'prototype');` / `  const DEFAULT_PAGE = opts.defaultPage \|\| '/知所栖-135-基础框架.html';`<br>监听在 `:445`：`    server.listen(PORT, HOST, () => resolve({ server, port: server.address().port, host: HOST }));`<br>CLI 壳 `scripts/serve-135.mjs:16`：`const { port } = await createZssServer({ root: ROOT, port: PORT, host: '127.0.0.1' });`（`:14` `const PORT = Number(process.env.PORT \|\| 5180);`）<br>桌面版 `app/main.js:62-63`：`  const s = await createZssServer({ root: ROOT, port: 0, dataDir: DATA_DIR,` / `    version: app.getVersion(), envFile: ENV_FILE, packaged: PACKAGED });`（不传 host → 落默认）<br>graph 回执 `:174`「**没有改认证、没有改监听范围**（仍然只有 127.0.0.1）」 |
| 判定 | **成立**，但要分清三层：<br>① 四个入口**都存在**，但**只在 `createZssServer(opts)` 这一层**，是**程序化参数**；<br>② **`host` 没有环境变量入口**——`serve-135.mjs` 把 `'127.0.0.1'` **硬编码**在调用里，`PORT` 才有 `process.env.PORT`；<br>③ `port` 默认值是 **`0`（系统随机空闲端口）**，不是 5180；5180 是 `serve-135.mjs` 给的。<br>**默认只监听 `127.0.0.1`（回环）——成立。** 全仓库未发现把 `host` 改成 `0.0.0.0` 或对外地址的调用点。 |

### B5. 上游 `dbskill` / `dbs-learning` 的许可证说法

| 项 | 内容 |
|---|---|
| 断言原话 | 「上游 `dbskill` / `dbs-learning` 的许可证说法」（任务书；来源不在本包）；任务书要求：**找不到就写「仓库内查不到，未核实」** |
| 仓库实际 | **① 没有许可证文件。** 检索命令与结果：<br>`find .agents -iname "*licen*" -o -iname "*copying*"` → **零命中**<br>`grep -rn -i "license\|许可" .agents/skills/` → **零命中**（无 SPDX 标识、无 `LICENSE`、无 frontmatter `license:` 字段）<br>**② 仓库根 `LICENSE:1` 是 MIT**，但只覆盖本仓库自有代码：「MIT License」/「Copyright (c) 2026 hou-152」；`THIRD-PARTY.md:3` 明说「下面这些材料**不适用 MIT**」。<br>**③ 仓库里有 4 处相关归属声明，其中两处互相矛盾，且都没写出具体许可证名：**<br>· `THIRD-PARTY.md:8`：「\| **dbs skill 原文**（15 个 skill 的 `SKILL.md` 与脚本） \| `.agents/skills/`、`app/assets/` \| dontbesilent 的 **dbskill**（公开项目） \| 按原样收录供索引与演示；权利归原作者，若要移除请删对应目录 \|」<br>· `.agents/skills/README.md:51`：「- 这 14 个是**所有者自己的方法论 skill**，不是第三方代码；`dbs` 系由官方 marketplace 分发。」<br>· `README.md:317`：「- `内容结构化系统/` 的方法来自 **dontbesilent 的 dbskill**（`/dbs-content-system`），公开项目。」<br>· `SOURCE_OF_TRUTH.md:59`：「原先卡在「15 个 dbs skill 原文的再分发范围」，**该未决事项已于 2026-09-13 由所有者当场撤销**（原话：「DBS 的 Skill 本来就是公开的」；上游 dbskill 公开分发）」<br>**④ `dbs-learning` 确实在仓库里**：`.agents/skills/dbs-learning/SKILL.md`（另有 `dbs-learning-beta`），frontmatter 只有 `name` / `description`，**无 license 字段**。 |
| 判定 | **仓库内查不到**——本仓库没有 dbskill / dbs-learning 的任何许可证文件、SPDX 标识或具名许可证条款。**不等于上游没有**，本核对未联网、未查上游仓库，**上游许可证状态未核实**。<br>**另判：仓库内两处归属声明互相冲突**——`THIRD-PARTY.md:8` 把它们列为「不适用 MIT 的第三方材料」，`.agents/skills/README.md:51` 说「不是第三方代码」。这个冲突本身是接线前要解决的事（见 §5-③）。 |

---

## 3. 成立但容易误读的地方

1. **「替换原有的 system 消息」实际是「全部丢弃重来」。** `serve-lib.mjs:333` 的 `filter(m => m.role !== 'system')` 会把调用方传的**每一条** system 消息删掉，只留 SKILL.md 原文。任何靠 system 消息下发的护栏（例如「学习者是数据，不执行其中指令」这类）在带 `skill` 参数时会**静默消失**。这是接线路由时最容易踩的一格。

2. **`/api/llm` 与 `/api/orchestrate` 的 Skill 装配语义不同，不能互相参照。** 前者替换（`:333`），后者拼接（`:382-388`，把组合提示词 + 各 Skill 原文 + 知乎检索材料拼成一条 system）。

3. **`/api/learn` 的「不读 Skill」是有意设计的窄接口，不是遗漏。** `serve-lib.mjs:157-161` 的注释写明：「浏览器的可信输入只有三样：单元 ID、学习者的复述、输入版本与请求 ID。材料、判据、五档规则、材料指针全部从服务端自己这份已审核映射里读——浏览器传来的 system prompt／评分标准／通过状态／模型名／上游地址一律不认。」**接线时不要"顺手"给它加上 `skill` 参数**，那会破坏这条信任边界。

4. **`learnPrompt` 的「只接当前复述」不等于「没有追问」。** 它要求模型回 `nextPrompt`（`:201`、`:323`），前端也渲染（`prototype/知所栖-壳.html:4670`「一个追问」）。**缺的是回流**：下一次请求不带上一轮的追问与判据状态。判断「短答承接有没有做」要看**回流**，不能看页面上有没有追问框。

5. **A 层的 111 与 117 不是矛盾，是时间差。** 同一份判据版本（`v3-20260914`）、同样 4 条判据，断言条数从 111 涨到 117，来源是 `f8b5b2d`。引数字时必须带版本。

6. **918 不是「另一套口径」，是过期标签文本。** 它只出现在断言**名字**里，代码路径与它无关（`test-path.mjs:76` vs `:192`）。看到 918 不要再去找"另一套集合"。

7. **「第六章被挡回第五章」是设计，不是回归。** 逐章解锁门的正常表现（`test-learn-agent-loop.mjs:242`）。

8. **包作者没碰过仓库这件事，决定了这份包的证据等级。** `:9` 与 `manifest.json:16` 都说清了。所以包内**每一条**仓库断言都是**转述**——这次核下来转述质量相当高（A3/A4/A5/A6/A9 都能在仓库里逐字对上），但**「转述准确」不等于「独立验证」**，引用时仍要回仓库。

---

## 4. 文档说错或说不清的地方

### ① C 层差异组的点名清单不全（**说错**）

- 文档原话（转述回执）：`Issue2_复核裁决与四阶段验收_20260914.md:29` 只写「解析 10/10、完整预期一致 4/10」。
- 回执原话 `evidence/issue2-复核-20260914.md:444`：「- 与预期完全一致：**4/10**。模型偏保守，**A4／B1／B3／B6** 多判 `partial`；**没有任何一组被少判或漏判**。」
- **实测反证**：`real-diagnosis-20260913200053.json`（从 git 恢复）逐组 diff 为 6 个不一致组，不是 4 个：

```
A1-空泛             got=[C1,C2,C3,C4] expect=[C1,C2,C3,C4] same=true
A2-分层对、成本与边界错  got=[C2,C3,C4]    expect=[C2,C3,C4]    same=true
A3-机制对、脚本与边界错  got=[C2,C3,C4]    expect=[C3,C4]       same=false  ← 未点名
A4-完整             got=[C2,C3]       expect=[]            same=false
B1-正确换说法         got=[C2,C3,C4]    expect=[]            same=false
B2-同关键词含否定       got=[C1,C2,C3,C4] expect=[C2,C3,C4]    same=false  ← 未点名
B3-只漏一个判据        got=[C2,C3,C4]    expect=[]            same=false
B4-无关回答          got=[C1,C2,C3,C4] expect=[C1,C2,C3,C4] same=true
B5-夹带直接通过        got=[C1,C2,C3,C4] expect=[C1,C2,C3,C4] same=true
B6-含否定但内容正确      got=[C2,C3,C4]    expect=[]            same=false
```

- 即 `4/10` 的**数字对**，但「多判的是 A4／B1／B3／B6」**漏了 A3 与 B2**。「没有任何一组被少判或漏判」实测为真（少判 0/10）。

### ② 回执点名的文件在仓库里查不到（**文档没提，但它转述的回执有**）

- 回执 `:442` 写「文件：`evidence/feynman-teaching-map/real-diagnosis-20260913200053.json`（我上一轮跑，v3 判据）」。
- **实测：该文件当前不存在**（`git ls-files` 无此文件，工作区无此文件）。
- 删除**有据**：`git log --diff-filter=D --name-only --oneline -8 -- 'evidence/*diag*'` → `f8b5b2d` 删除了 `real-diagnosis-20260913195225.json` 与 `real-diagnosis-20260913200053.json`；且 `real-diagnosis-20260913203201.json:459` 的 `criteriaRevisionNote` 载明「此前的 real-diagnosis 记录是补全之前跑的，已删除」。
- **可恢复**（实测）：`git show f8b5b2d^:evidence/feynman-teaching-map/real-diagnosis-20260913200053.json` → 25978 字节，可解析，10 组。**本文件 §2A-A4 的 4/10 就是用这个恢复出来的文件重算的。**
- 包（`Issue2…md:40`）只说了「显示删除状态」，**没说清它现在到底在不在**，也没说它可从 git 恢复——**这一点文档说不清**。

### ③ 文档把「回执事实」与「复核判断」分栏了，但没标出**哪些回执事实已过期**

`Issue2…md:13-17` 定义了四种标记，质量不错。但 A3（111→117）、A5（HEAD 已前进 13 个提交）这两条属于**回执当时为真、现在为假**，文档没有任何机制提示这件事（因为它自己就声明没读仓库）。**引用时必须带日期。**

### ④ 「8/10 复测一致」的口径没写清

- 回执 `:445` 只写「同条件 8/10 一致」。
- **实测**：按 missing 集合比 = 8/10（差 B1、B6）；**按四档 status 完全相等比只有 7/10**（另差 A2，是 `missing`→`partial`）。
- 文件内 `sameConditionRunNote` 逐字点名的是 B1、B6，用的是 missing 口径。**文档转述时把口径丢了**，读者无法判断 8/10 是什么的一致率。

### ⑤ 「另报 8/10」与另一份 11 组记录容易被混用（**文档没提**）

- `real-diagnosis-20260913211128.json` 是**更晚一轮**（21:10）、**11 组**（多 B7-明确说没读懂）、新 schema（含 `expected` + `observed.statuses`）。
- **实测：11/11 解析、5/11 完全一致。**
- **这是第三个数字。** 包在 `:85` 明确警告「不要把规则注入前的结果与注入后的结果合并成一个比例」——这条警告是对的，但它没有指出仓库里**已经存在**一份 11 组记录，照字面引用极易把 4/10 与 5/11 混成一个比例。

---

## 5. 文档没提、但接线时必须处理的

### ① 非法档位值的归属（复核要求 vs 代码现状**不一致**，且改动会碰公共判定口径）

- 复核要求（`:60`）：「模型返回协议外状态、重复 ID、缺项、版本/请求关联错误或不可解析内容，属于系统 `notJudged`，不能伪装成学习者表达含糊。」
- **代码现状（实测）**：

| 情形 | 代码 | 行 |
|---|---|---|
| 解析失败 / 无 criteria | `notJudged` | `serve-lib.mjs:232-234` |
| 未知判据 ID / 条目缺 id | `notJudged` | `serve-lib.mjs:236` |
| 判据不全（重复 ID 会在此暴露） | `notJudged` | `serve-lib.mjs:238` |
| **协议外档位值** | **降级为 `uncertain`** | **`serve-lib.mjs:242`** |

- `:242` 逐字：`      return { id, status: LEARN_STATUSES.includes(s) ? s : 'uncertain', evidence: String(row.evidence || '').slice(0, 300) };`
- 而回执 `:145` 那条断言（`test-neican-inline-feynman.mjs:204`）**把「降级为 uncertain」当成期望行为来验**。
- **结论：这是 4 条里唯一「文档要求 ≠ 代码现状」的点**，且复核要求与现有测试**方向相反**。要动就得**同时改代码和那条断言**，并按 `:65` 补一条「非法协议测试与真实 uncertain 测试分离」。**本轮未改任何代码。**

### ② Skill 装配会静默丢掉调用方的 system 护栏

见 §3-①。接线时若走 `/api/llm` + `skill`，**必须自己确认护栏是不是被丢了**——目前代码不会报错、不会提示，只是消失。

### ③ dbskill 的归属 / 再分发口径在仓库内自相矛盾

- `THIRD-PARTY.md:8`：列为「不适用 MIT」的第三方材料，权利归原作者。
- `.agents/skills/README.md:51`：「不是第三方代码」。
- 两处都没有具名许可证。
- `SOURCE_OF_TRUTH.md:59` 记了所有者 2026-09-13 的口头撤销（「DBS 的 Skill 本来就是公开的」）——**这是所有者的判断，不是许可证文本**。
- **接线前应把这三处统一**，否则任何「能不能随包分发」的判断都缺少可引用的依据。本轮只标注冲突，不擅自定版本。

### ④ `/api/learn` 没有任何会话/短答承接通道

见 §2B-B3。若产品要求「短答承接」（上一轮追问 + 上一轮判据状态回流），**当前接口形状做不到**：请求体只有 `unit` / `said` / `inputVersion` / `requestId`（`:287-290`），服务端无状态。这属于**接口设计变更**，不是参数微调——本包与回执都没把它列进四阶段任何一档。

### ⑤ 限流是进程内计数，不能当配额

`serve-lib.mjs:65-68` 自述：「照实说明这是**进程内**计数：它只对这一个本地服务进程成立，挡的是误触与失控重试，不是分布式配额，也不能冒充可靠的全局限额」。做 G4 压测时**不能**把这个当限流能力来报。

### ⑥ `dbs-learning` 与 `dbs-learning-beta` 是两个不同的 Skill，别混

`.agents/skills/README.md:22` 明确分流：`dbs-learning` 走「模块 1 互动阅读器」，`dbs-learning-beta` 走「没有标准答案」的入口分流。`serve-lib.mjs:398` 的注释与 `:21-22` 的注释引用的都是 `dbs-learning-beta` 的产物路径。核许可证时这两个要分开核。

---

## 6. 断言与仓库实际的差异清单（git 取证，不许凭印象）

> 全部命令可原地重跑。本节只读，未产生任何提交。

### ① HEAD `aa9ac38` 与当前 HEAD 的关系

```
$ git rev-parse --short HEAD
4dd73f0
$ git rev-parse --abbrev-ref HEAD
main

$ git cat-file -t aa9ac38
commit
$ git merge-base --is-ancestor aa9ac38 HEAD && echo YES
YES                                  # aa9ac38 是 HEAD 的祖先
$ git rev-list --count aa9ac38..HEAD
13                                   # 落后 13 个提交
$ git log -1 --format="%H %ad %s" aa9ac38
aa9ac3820aaba154aa6109226c4e8f8076431528 Mon Sep 14 04:01:01 2026 +0800 fix(feynman): C2 按负责人裁决改 v3（只考占用与加载的关系）· 解析失败不再降级 · JSON 提取修复后 10 组零未判定
```

| 断言 | 判定 |
|---|---|
| 「§1 的 HEAD 是 `aa9ac38`」 | **成立但已过期**。`aa9ac38` 真实存在且是祖先，但当前 HEAD 是 `4dd73f0`（`main` 分支），相隔 13 个提交。**不能再拿 `aa9ac38` 当"这次测过的版本"。** |

### ② 「工作区包含大量并行修改」

```
$ git status --porcelain
?? "知所栖单篇MVP-交付包-20260914.zip"
$ git status --porcelain | wc -l
1
```

| 断言 | 判定 |
|---|---|
| 回执 `:22-53` 列了 30 行 ` M` / ` D` | **当时成立**。本核对**开始时**（2026-09-14 07:0x）实测工作区**只有 1 个未跟踪文件**（一个 ZIP），其余全部已提交。 |
| **但本核对进行中（07:13–07:15）工作区又被并行写入** | 实测 `git status --porcelain` 变为：4 个 `prototype/预览/*.png` 与 1 个 `output/graph-shots/*.png` 被 ` M`，并新增未跟踪项 `scripts/build-batch-units.mjs`（07:14）、`scripts/check-batch-units.mjs`（07:15）、`scripts/lib/batch-units-rules.mjs`（07:14）、`evidence/batch-units-260914/units.json`（07:14，1.39 MB）。**这些不是本核对创建的**（本核对只新增 `docs/` 下三个文件，且未改 `scripts/`、`evidence/`、`prototype/`）。内容指向 HEAD 提交 `4dd73f0` 那份「76 个 CON → 可运行单元」施工单——**另一个会话正在本工作区执行它**。 |
| 综合判定 | **R1 担心的「HEAD 不能单独标识这次通过测试的完整工作树」——这个风险不但没有消失，此刻正在发生。** 头部提交 `4dd73f0` 未变（本核对未提交任何东西），但**工作树在分钟级变化**。冻结版本这件事必须按「提交号 + 文件 sha256」双轨来记，不能只看 `git status`。 |

### ③ 两份旧诊断 JSON 的删除与去向

```
$ git log --diff-filter=D --name-only --oneline -8 -- 'evidence/*diag*'
f8b5b2d feat(neican): 260913 期内参（8 篇三产物 + 81 概念并入地图 918→999）+ 内参栏改多期「日报集合」（月历 → 分类 → 策展）
evidence/feynman-teaching-map/real-diagnosis-20260913195225.json
evidence/feynman-teaching-map/real-diagnosis-20260913200053.json

$ git ls-files evidence/feynman-teaching-map/
（当前跟踪的：agent-skills-api.json / inline-feedback-*.json ×4 / real-diagnosis-20260913203201.json / …203426.json / …211128.json）
$ git status --porcelain evidence/feynman-teaching-map/
（空）
```

| 断言 | 判定 |
|---|---|
| 「两份旧诊断 JSON 显示删除状态」（回执 `:35-36`） | **当时成立**（` D`，工作区删除待提交） |
| 现状 | **已从 HEAD 删除**，删除落在提交 `f8b5b2d`。**但两份都可从 git 恢复**（实测）：<br>`git show f8b5b2d^:evidence/feynman-teaching-map/real-diagnosis-20260913195225.json` → 24231 B<br>`git show f8b5b2d^:evidence/feynman-teaching-map/real-diagnosis-20260913200053.json` → 25978 B |
| 「其中一份又被 §4.2 引作结果依据」 | **成立**。`:442` 引的正是 `…200053.json`（25978 B 那个，可从 `f8b5b2d^` 或 `aa9ac38` 取回）。**复核 R1-3 要求的「仍在 Git 时给可解析的定位」——本行即定位。** |

### ④ 918 / 999

见 §2A-A9（已结案）。补一条 git 侧证据：`f8b5b2d` 的提交标题自己就写着「81 概念并入地图 **918→999**」，与 `scripts/test-path.mjs:18-19` 的注释一致。

### ⑤ 图原型那笔「本地提交 `ad61d29`」**不在 HEAD 历史里**

```
$ git log -1 --format="%h %ad %s" ad61d29
ad61d29 2026-09-14 feat(graph): 全链路 Graph 原型（材料→语义→知识→课程→活动→记录，图驱动实际学习）
$ git merge-base --is-ancestor ad61d29 HEAD && echo YES || echo NO
NO
$ git branch -a --contains ad61d29
（空 —— 没有任何分支或远端引用包含它）

$ git log --oneline -2 --diff-filter=A -- scripts/lib/graph-runner.js
094e33c feat(graph): 全链路 Graph 原型（材料→语义→知识→课程→活动→记录，图驱动实际学习）
$ git merge-base --is-ancestor 094e33c HEAD && echo YES
YES
```

- `git rev-parse ad61d29^{tree}` = `321872f3…`　vs　`git rev-parse 094e33c^{tree}` = `a3afec38…` → **整棵树不同**（ad61d29 被重写）。
- **但总图那几个关键文件内容逐字节相同**（实测 sha256 前 16 位）：

| 文件 | ad61d29 | HEAD |
|---|---|---|
| `scripts/lib/graph-runner.js` | `9bdd3e01849f142c` | `9bdd3e01849f142c` |
| `scripts/lib/graph-adapter.mjs` | `b67e74fd9ad1748e` | `b67e74fd9ad1748e` |
| `knowledge/graph-260914/graph.json` | `af5ce4f19e7fe529` | `af5ce4f19e7fe529` |

| 断言 | 判定 |
|---|---|
| graph 回执 `:47`「本地提交 `ad61d29`」 | **该 SHA 现在指向一个不可达提交（悬空对象）**，没有任何分支包含它。**内容没丢**——同一份图工作以 `094e33c`（HEAD 的祖先，2026-09-14 07:01:10）进入主线，关键文件字节相同。**引用时应改引 `094e33c`，或引文件 sha256。** |

### ⑥ A 层断言数 111 → 117

```
$ git diff --stat aa9ac38 HEAD -- scripts/test-feynman-teaching-map.mjs evidence/feynman-teaching-map/agent-skills-api.json
 .../feynman-teaching-map/agent-skills-api.json     | 39 ++++++++++++++++++----
 scripts/test-feynman-teaching-map.mjs              | 14 ++++++++
 2 files changed, 47 insertions(+), 6 deletions(-)

$ node scripts/test-feynman-teaching-map.mjs | tail -1     # 实测
（输出「费曼教学映射验收：117 项通过」，exit 0）
$ node -e '…' # criteriaVersion / criteria.length
at aa9ac38: v3-20260914 | criteria: 4
at HEAD:    v3-20260914 | criteria: 4
```

| 断言 | 判定 |
|---|---|
| 回执与包都说「111 项」 | **当时成立；现为 117。** 判据版本与判据条数都没变，只是断言变多。**引数字必须带提交号。** |

### ⑦ 本文件新跑的两条确定性命令（实测）

```
$ node scripts/check-graph.mjs
① 结构：断链与必填字段
…
通过 85 · 失败 0
--- exit: 0 ---

$ node scripts/test-feynman-teaching-map.mjs
费曼教学映射验收：117 项通过
✅ 不同理解缺口 → 不同教学动作，已证明
--- exit: 0 ---
```

两条都是**只读、不需 serve、不调模型**。`check-graph.mjs` 的 85/0 与 graph 回执 `:154` 记的「85 项」一致。

---

## 附：本文件未核实到的部分（照实标注）

1. **上游 dbskill / dbs-learning 的许可证状态**——未联网、未查上游仓库，只核了本仓库。§2B-B5 的判定是「仓库内查不到」，不是「上游没有」。
2. **`test-graph.mjs`（210 项）、`test-graph-page.mjs`（40 项）、`test-learn-agent-loop.mjs`、`test-path.mjs`、`check-learning-materials.mjs`、`test-neican-inline-feynman.mjs` 本轮未复跑**。它们中有多个需要 serve 或需要无头 Chrome。本文件对这些的判定基于**仓库内的回执文件**（`evidence/issue2-复核-20260914.md`、`evidence/graph-260914/回执-全链路Graph-20260914.md`）**加静态读码**，**不是本轮实测**。凡属此类，行内已写明「回执原话」。
3. **`walk-graph-real.mjs`（真模型走查 11/0）本轮未复跑**——它真实调用模型，按硬边界禁止。判定基于 `evidence/agent-loop-260913/graph-walk-real-20260913225803.json` 与 graph 回执 `:135`。
4. **D 层三轮真实反馈的语义内容**未逐轮重读原始 JSON——只核了轮次结构、断言行数与文件存在性（实测），以及回执的转述。
5. **`real-diagnosis-20260913200053.json` 是从 git 恢复的副本**（`git show f8b5b2d^:…` 输出到 `/tmp`），不是工作区文件。判「4/10」用的是这份恢复副本 + 现存的 `…203201.json` / `…203426.json`，三者一致。
6. **本核对进行中有另一个会话在同一工作区写入**（见 §6-②，07:13–07:15 新增 `scripts/batch-units-*` 等）。**那批文件本文件一件都没读过、没跑过、没验证过**——不归本文件判断。
7. **「本核对开始时工作区干净」这句话有时效**。§6-② 的第一个判定是 07:0x 的快照；引用时请带上这个时间。
8. **`/api/graph` 的 `?kind=` / `?layer=`** 只做了静态读码（确认无对应分支），未起服务实测。
9. **所有 HTTP 调用示例均未实跑**——本核对未起 serve、未调模型。示例是按代码构造的。
