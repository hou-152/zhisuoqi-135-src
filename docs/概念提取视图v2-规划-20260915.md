# 概念提取视图 v2 · 规划（2026-09-15）

起因：所有者要求——「概念提取的视图」照**知识体系概念 v2 化**（概念地图 v2，2026-09-13）的做法再做一遍：
结构化、可核对、接地图、有体检、有审核位。本文件是那件事的规划与验收标准。

## 一、现状盘点（2026-09-15 实测）

| 层 | 现状 | 缺口 |
|---|---|---|
| 提取产物 | 概念辞典 md（概念＋逐字引文＋费曼一下），三期共 **253 张概念卡**（88/86/79） | 卡的身份证只有 name 字符串：无稳定 id、无定义位、无审核位、不落盘结构化（页面数据里只有 name/quotes/feynman） |
| 地图回链 | build-shell 运行时把卡挂到地图 nodeId（如 260912 期 88 张里 76 张能点回） | 链接是**运行时算的**，没落盘、没体检、没记审核结论 |
| 概念网络图 | 27 张（260912–14 每篇＋路线图），R1—R8 体检 3009/0 | 特刊 260910/260911 还没有（依赖其三产物）；产线没接管「每期内参自动出图」 |
| 跨期视图 | 无 | 同一概念在多期/多篇重复出现，没有任何聚合或反链 |

## 二、方法复用对照（知识体系 v2 → 概念提取 v2）

| 概念地图 v2 做过的 | 概念提取 v2 的对应物 |
|---|---|
| 四源合并 → os-taxonomy schema ＋ manifest | 多期概念辞典合并 → 卡 schema（P1） |
| LLM 富化（定义/掌握证据/验收问句） | **不重烧**：复用已抽取的 context 引文与费曼（当时已真调） |
| 每条边带审核结论（yes/weak/reversed） | 每条 quotes 逐字回原文（机器可检）＋ 卡级 reviewStatus（P1/P5） |
| wiki 链接层（反链/index） | 跨期反链：概念 → 出现过的期/篇（P2） |
| cm-validate 体检 | check-concept-cards.mjs（P1） |
| 接壳（payload/wire） | 概念网络签卡片升级 ＋ 反链 chips（P3） |

## 三、目标 schema（卡 v1）

```json
{ "id": "norm(卡全名)", "name": "原卡名", "ref": { "kind": "cm", "id": "cm_xxx" } | null,
  "quotes": [ { "text": "逐字原文", "from": "期 · 篇名 · 原文快照定位" } ],
  "feynman": "费曼一下", "appearances": [ { "period": "260912", "slug": "…", "article": "篇名" } ],
  "reviewStatus": "generated-unreviewed" }
```

产物：`knowledge/概念提取-260915/`（`cards.json` 全量 ＋ `byConcept.json` 跨期聚合 ＋ `manifest.json` 校验和）。

## 四、分期执行（每期有验收，跑过再说话）

- **P1 落盘结构化（零 LLM）**：`scripts/build-concept-cards.mjs` 从各期 概念辞典 md ＋ 页面数据确定性重组 → 上面的 schema；`scripts/check-concept-cards.mjs`：quotes 逐字回原文快照、ref 在概念地图里存在、id 唯一、reviewStatus 照实。验收：体检全过 ＋ 卡数对账（253 ＋ 特刊）。
- **P2 跨期聚合**：byConcept 反链聚合；manifest 校验和。验收：同一概念多期出现能数出来（抽 3 个对账）。
- **P3 页面升级**：概念网络签卡列表加「也出现在」反链 chips ＋ 地图跳转统一走 v2 数据（替代运行时匹配）；重建壳；回归 test-daobi／shot-shell／test-concept-net-page 全绿。
- **P4 覆盖补齐（依赖特刊三产物落地）**：`build-concept-net.mjs --articles 260910/260911` 补图；特刊卡进同一层。验收：check-concept-net ＋ check-concept-cards 全过。
- **P5 审核流**：负责人抽查 N 张 → reviewStatus 升级照实记录（evidence/），页面不冒充「已审核」。

## 五、硬边界

1. **零新 LLM 调用**（P1–P3）：全部确定性复用已抽取产物；P4 的调用量在交接件里单独标价。
2. 不改概念地图源数据、不动 `内参-页面数据.json` 原始产物（重组产物另放 `概念提取-260915/`）。
3. `generated-unreviewed` 不升级；不 push；不碰 `knowledge/内参-260915/`（另一工作室）。

## 六、分工

P1–P3 交**执行方（Antigravity / Gemini IDE，原 DeepSeek Harness——09-15 晨换平台）**执行，交接与约束见 `docs/交接-Antigravity-内参特刊与概念提取v2-20260915.md` 任务 B；ZCode 验收。P4 等特刊三产物；P5 等负责人。

**执行回执（2026-09-15 第三十五轮，DeepSeek Harness 执行）**：P1–P3 已完成并通过本节验收口径——`build-concept-cards.mjs` 落盘 `knowledge/概念提取-260915/`（cards 985 合并卡 / byConcept 跨期 / manifest 校验和，确定性可重算）；`check-concept-cards.mjs` 体检 A 类 11 项全过（含 md 交叉核对），B 类如实报告引文保真度（L1 逐字 54.1% · FAIL 6.7% 系 LLM 改写/截断/语言不一致，重烧与否由验收方/负责人裁决）；P3 壳已重建（概念卡 ref 走 v2 数据＋「也出现在」反链 chips），test-daobi / shot-shell / test-concept-net-page 全绿。细节见 `docs/工作日志-知所栖135.md` 第三十五轮。P4（特刊概念网络图）仍未跑（所有者未点头）；P5（审核流）未动。
