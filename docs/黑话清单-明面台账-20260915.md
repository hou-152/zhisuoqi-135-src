# 黑话清单：明面 / 台账（2026-09-15 · v5-plain-open phase 03）

**口径一句话**：学习者明面说人话；内部口径不删——退到折叠区、数据层属性（data-*）、`CONTEXT.md`。黑名单已写成断言（test-path ⑬，四面扫描），防回潮。

## 已下架（明面 → 替换口径）

| 黑话 | 明面替换 | 出处去向 |
|---|---|---|
| `cm_*` / `CON-*` / `QST-` / `CAS-` / `OPI-` / `SOL-` | 不显示；材料标签改人话（这一章的问题/核心概念/场景/判断依据/怎么做） | `data-src` / `data-basis` / `data-checks` / `data-case` |
| `L0–L5 层级`、前置/解锁数字孤岛 | 不显示；关系列表带「强/弱＋一句理由」 | `data-level` / `data-pre` / `data-post` |
| `CONCEPTUAL` 等类型枚举 | 徽标用中文名（能判的/能用的…） | `data-type` |
| 倒逼 · 你不能自己标「学过」 | 讲一遍 · 我来挑漏（讲完了，挑漏） | — |
| 验收问句 / 掌握证据 | 怎么算会了 | 更多判据折叠 |
| hard / soft | 强 / 弱 | `DATA.edgeMeta` |
| 待验证区分度 / 已激活 | 还没被真人验证 / 经过了真人验证 | 审核对账折叠（原词保留） |
| superseded | 这一讲在主线六章里 / 已被手工章节取代 | 台账与数据层 |
| verdict=usable / indexOf+sha256+locator / ready / scaffold / gradingRules / not-met | 出处「登记在案，可逐条复算」 | `data-origin`、审核对账折叠 |
| 机器派生补充判据 | 补充判据（只作复习提示，不计分） | `data-origin` |

## 声明的例外（台账面，token 原样保留）

- 状态看板（`openPracticeBoard`）与「审核与对账记录」折叠（`#practice-qa-fold`）
- blocked 理由（点了不能进的单元才出现）：缺什么照实写，含数据侧原词
- `iterations/` 各档 `.plan/` 与证据文件、`CONTEXT.md` 本身

## 已知残留（待裁决）

- authored 正文 1 处「以 CON-agent 的行为口径为准」（负责人审过的材料文字，不静默改——test-learn 显式白名单）
- 画布右缘 L0–L5 层级尺（2026-09-15 已裁决 · 选项 C）：坐标轴符号**保留、不翻译成句子**，但必须配钥匙。
  钥匙语义（已核，源 `scripts/build-concept-map.mjs:58-70`）：L＝前置链的最大深度——L0 是没有任何前置的源头概念，
  L 越大＝要先多懂几层才能到这里；它量的是「离源头多远」，不是难度。
  交接注记（给知识树/系统视图定稿会话）：定稿时层级尺须附一行图例或悬停说明（建议文案：「右侧刻度＝从源头概念数起要过几步（L0 是源头）」）；
  在那之前，渲染文件不动（避免与并行在途改动撞车），黑名单断言继续豁免画布装饰。
- 并行会话 v4-unify 在途区域（顶栏更新面板/探索）本轮未扫，避免踩他们正在写的文案
- 公网版未重建：模板里 v4-unify 半成品会被烘进去；等 v4-unify 落库后统一 build（改文件 ≠ 发布）
