# 交接 · DeepSeek：AI 内参 260915 期全链收尾（2026-09-15）

> **⚠ 已作废（2026-09-15）：所有者改派 Antigravity 接手，现行版＝`docs/交接-Antigravity-内参260915-20260915.md`，以那份为准。本文件仅留档。**

> 接手人：DeepSeek。本文件自包含，照顺序执行即可。硬边界照仓库根 `AGENTS.md`（push／发布前先一句话确认；凭证不展示不入库；改完必须跑验收再说话）。
> 背景：所有者指令「今天入库的 Readwise → 三级笔记/概念提取/费曼 → 贴进黑客松网站 → 放进 DaCapo 的 Notion 当后端」。前半已由 GLM 会话完成，你接的是后半。

## 一、已完成（勿重做、勿覆盖）

1. **Readwise 已拉**：`knowledge/内参-260915/issue.json`（**10 篇**，2026-09-15 当天 Inbox，按 `saved_at` 严格筛；同期 RSS 48 条未纳入）＋ 原文快照 `knowledge/内参-260915/原文/*.md` ＋ 候选凭证 `evidence/内参-260915/readwise-原始返回.json`。
2. **第 1 篇三产物已完成**（GLM 按 note-taking-pro / concept-learning SOP 直写，31 条引文程序化逐字核对全命中，**保留勿覆盖**）：
   - `knowledge/内参-260915/三级笔记/jacobpeake-ai-chip-architectures.md`（40,142 B）
   - `knowledge/内参-260915/概念辞典/jacobpeake-ai-chip-architectures.md`（16,025 B，14 概念＋Mermaid 架构图）
   - `knowledge/内参-260915/AI费曼/jacobpeake-ai-chip-architectures.md`（348 字）
   - `evidence/内参-260915/agent-meta/jacobpeake-ai-chip-architectures.json`（stars=4 等策展元数据）
3. **Notion 落库脚本已写好**：`scripts/neican-to-notion.mjs`（幂等、按 source link 查重；目标＝**DaCapo · serious AI 空间**，所有者 09-15 指定：日报库 `serious AI 日报 ｜daily` 一行一期 + 阅读库 `serious AI 阅读库｜reads` 一行一篇；字段映射见脚本头；ntn CLI 已登录）。
4. 之前误启的 `build-neican-daily` 进程已 kill，无残留；`三级笔记/概念辞典/AI费曼` 三个目录里目前只有 jacobpeake 一套文件。

## 二、你要做的（按顺序）

### 第 1 步：补齐其余 9 篇三产物

首选跑既有流水线（`cached()` 会自动跳过 jacobpeake 已有文件）：

```sh
node scripts/build-neican-daily.mjs --issue 260915 --concurrency=3
```

- 已知坑：DeepSeek 思考型模型单次 2–6 分钟，**文件按篇写盘（三产物齐了才写）**，脚本**没有 HTTP 超时**——进程长时间无产出先 `lsof -p <pid> | grep -c TCP` 看连接是否在传，再决定等或杀。
- 失败记账落 `knowledge/内参-260915/编辑日志.md`，不静默吞。
- 若流水线某篇反复失败，退路＝按 SOP 直写该篇（system prompt 用 `~/.agents/skills/note-taking-pro/SKILL.md` 与 `~/.agents/skills/concept-learning/SKILL.md` 原文，费曼 SOP 在 `scripts/build-neican-daily.mjs` 头部 `SOP_FEYNMAN`；**引文必须逐字**，产出路径 `knowledge/内参-260915/{三级笔记,概念辞典,AI费曼}/<slug>.md`）。格式样例看 260914 期同名目录。
- 待补 9 篇（slug ｜ 字节 ｜ 标题）：
  1. `thezvi-gpt-6-astra-can-do-ambitious-things` ｜ 82,250 ｜ GPT-6-Astra 能做很多雄心勃勃的事情
  2. `github-hyperresearch` ｜ 34,771 ｜ hyperresearch 智能体研究知识库
  3. `pwning-llm-memory-program-analysis` ｜ 29,592 ｜ Some things should probably stay fuzzy
  4. `x-2090252351533973768` ｜ 14,971 ｜ Building software factories (with no slop)
  5. `openai-introducing-the-agents-api` ｜ 13,592 ｜ OpenAI Agents API
  6. `learn-git-worktrees` ｜ 13,037 ｜ Worktrees ｜ ChatGPT Learn
  7. `claude-how-warp-builds-self-improving-agents-on-claude` ｜ 11,794 ｜ Warp 自我改进 Agent
  8. `mp-bsn57d` ｜ 7,084 ｜ 谷歌 WikiSkill
  9. `terrytao-deep-theorems-were-scarce-and-difficult-and-so-beca` ｜ 3,377 ｜ Terence Tao 论深奥定理

### 第 2 步：预置元数据缓存，再出页面数据

```sh
# 把已完成的 agent-meta 合进缓存（缺失的 slug build-neican 会真调 LLM 补，带缓存）
node -e '
const fs=require("fs");
const dir="evidence/内参-260915/agent-meta";
const p="knowledge/内参-260915/内参-元数据.json";
const cache=fs.existsSync(p)?JSON.parse(fs.readFileSync(p,"utf8")):{};
for(const f of fs.readdirSync(dir)){ if(!f.endsWith(".json"))continue;
  const j=JSON.parse(fs.readFileSync(dir+"/"+f,"utf8"));
  const {slug,...rest}=j; if(slug) cache[slug]=rest; }
fs.writeFileSync(p,JSON.stringify(cache,null,1));
console.log("seeded",Object.keys(cache).length,"条");'
node scripts/build-neican.mjs --issue 260915     # → knowledge/内参-260915/内参-页面数据.json
```

### 第 3 步：概念并图 + 壳 + 公网重建

```sh
node scripts/cm-add-neican.mjs --issue=260915   # 概念并进地图，改前自动备份 .pre-neican-260915.bak
node scripts/cm-validate.mjs                    # 地图体检
node scripts/cm-wire.mjs                        # 重出壳 payload（概念数变了必须重跑）
node scripts/build-shell.mjs                    # → prototype/知所栖-壳.html（DATA.neican.issues 装全部期）
node scripts/build-public.mjs                   # → deploy/zhisuoqi-135/index.html（改文件≠发布）
```

### 第 4 步：验收（跑过再说话）

```sh
node scripts/serve-135.mjs &                    # 5180
node scripts/check-public.mjs                   # 37 项（v3 知识树口径）；必须假域名，用法见脚本头（自起 5199）
node scripts/shot-shell.mjs                     # 21 步截图 + 0 JS 报错
node scripts/test-daobi.mjs && node scripts/test-path.mjs   # 回归（44 / 110 项）
```

### 第 5 步：Notion 落库（DaCapo serious AI 空间）

```sh
node scripts/neican-to-notion.mjs --issue 260915 --dry-run   # 先看计划
node scripts/neican-to-notion.mjs --issue 260915             # 真写：日报行 + 10 个阅读行
```

- 效果：日报库新增「260915 期」（agent=内参编辑 agent · 阅读进度 Done）；阅读库每篇一行（正文＝三级笔记＋概念辞典＋AI费曼原文；进度=2.2 已笔记+概念；概念提取+入库 ✓；🐻 日报 双向关联自动同步）。
- 幂等：中断直接重跑，已存在的行按 source link 跳过。跑完打印每行 URL，人工抽查 1–2 行。

### 第 6 步：发布与登记（**先向所有者要一句确认再 push**）

1. `deploy/zhisuoqi-135/` → 公网仓库 `hou-152/zhisuoqi-135` main push（线上即更新；本地当前还压着 09-15 图谱/顶栏改动，会一并上线，均验过）。
2. 源仓库提交本轮全部产物（内参 260915 全链 + 概念并入 + 脚本），push 前扫一遍无 `.private/` 内容。
3. `docs/工作日志-知所栖135.md` 登记（第三十五轮或紧接当时最新轮）：做了什么／数字（本期 N 篇 · 概念新增 M 节点 K 边 · tokens）／验收脚本结果／**如实注明**：jacobpeake 一套三产物为 GLM 直写，其余 9 篇为 DeepSeek 流水线产物，均「LLM 生成、未人核」。
4. `SOURCE_OF_TRUTH.md` 内参行与产物表数字同步（内参期数、概念地图 topics/dependencies 新数）。

## 三、红线提醒

- 三产物引文必须能在原文里逐字找到（`indexOf` 级）；引号内只放逐字原话。
- Notion 目标库认准 DaCapo 的 serious AI 空间（日报 `2cc679b1-08ff-834b-9acc-07819069899b` / 阅读 `d52679b1-08ff-825d-99b9-87bb06e191a7`），**不是**「English 行动库」。
- push／公网发布前必须所有者确认；`.private/` 不入库不展示；不伪造验收结果。
