# 开工单 · 知所栖 135（一页，发群用）

> 09-13 02:5x 建。**只写「谁今天交什么、几点、怎么算通过」。** 细节读 `docs/前端契约-知所栖135.md`（字段）与 `docs/接口机制-麦肯锡式-知所栖135.md`（谁交给谁）。
> 一页纸规则是自己定的：超过一页就是没想清楚。

---

## 一、形态一句话（这是我们和「多邻国」的区别）

**不是「看完一篇文章」，是一次一题、答完立刻判、每题都能点回原文。**
多邻国式的节奏（一次一题、即时反馈、有进度），**知乎的内容做底**（每个判定都给出处，出处点得回去）。
五个屏是一条链：**读 → 判 → 做 → 讲**。做完不算完，**用自己的话讲一遍才算**（费曼验收＝限速器）。

> 这句话此前**没写进任何一份文件**：文档里冻结的是**步骤**（1 阅读 → 3 决策 → 5 实验 → 费曼验收），
> 没冻结**交互形态**（一次一题 / 即时判 / 可回溯出处）。这三条才是崇铭要讲的、肖力臣要做的、却白要供内容的同一句话。
> 现已写入 `docs/前端契约-知所栖135.md` 顶部，作为契约第一条。

---

## 二、四个人，各自今天的第一件事

| 人 | 角色 | 今天要交的（可指认的产物） | 死线（绝对时间） | 怎么算通过 |
|---|---|---|---|---|
| **capo** | 核心技术负责人 | ✅ **已交**：`evidence/mock/`（真结构假内容，含 5 屏对照 `screens.md`） | 09-13 02:5x 已交 | `node scripts/verify-mock.mjs` → **59/59** |
| **肖力臣** | 前端 / 交互负责人 | 5 屏做进壳里：改 `scripts/shell.template.html`，加一行 mock 脚本即可**离线开工**（不用等任何接口） | **09-14 00:00**（= 09-13 24:00，冻结线①） | `node scripts/shot-shell.mjs` + 自己把 5 屏点一遍 |
| **崇铭** | 产品负责人（**= 最终讲解 = 主负责人**） | ① 群里落字认领「主负责人」② `docs/JTBD-填空版-知所栖135.md` 6 个空 ③ 砍掉清单 | 认领：越快越好（建议 09-13 内）；其余 **09-14 10:00**（冻结线②） | 群里那一行字 + 文件里填完的 6 个空 |
| **却白** | 集成 / 内容 / 验证负责人 | ① **「内容」≠「文库」**划清（两份不同的东西，群里别用同一个词）② 演示脚本骨架填实 | 09-14 00:00 | `docs/演示脚本-知所栖135.md` 里**每条命令、每个地址本人都跑过一遍** |

**卡住找谁**：任何一条卡超 30 分钟，直接 @ 那条边的 A，不要自己扛（12 条边见接口机制 §三，已按上面的真名对齐）。

---

## 三、三件必交、但**现在没有名字**的东西（这是最大的风险）

| 材料 | 要求 | 现状 | 建议归属 |
|---|---|---|---|
| **产品说明计划书** | **必交**，且是初审「重点考核项」 | ❌ 无草稿 | 崇铭（对外叙事是他的选择标准），**需要他当场认领** |
| **135 主流程进公网版** | 公网 Demo 是必交件 | ⚠️ 线上是**壳**，5 屏主线**不在里面**（线上 0 处「本次尚未验收」） | capo 出构建，却白验收 |
| 项目演示视频 | 选交，可后补 | ❌ 零进展 | 却白（内容侧）＋ 崇铭（分镜） |

---

## 四、三条不准踩的线（其余自由）

1. **契约只加不改**：`docs/前端契约-知所栖135.md` 的字段名与含义已冻结；要改 → 先说一声 → 同一次改动里更新契约。
2. **产物不手改**：`prototype/知所栖-壳.html`、`prototype/知所栖-135-公网版.html` 是**构建产物**，手改一律回滚。
   `prototype/知所栖-135-基础框架.html` 是**手改源文件**（5 屏现在住在它里面），可以改。
3. **说了不算，脚本说了算**：任何人说「改好了」，必须附上相关验收脚本的输出。**只有截图不算**（截图可以截上一版）。

---

## 五、还缺的**唯一一个输入**（缺它，分工只能停在「谁负责」）

> **未来 48 小时里，你各自能实打实投入的是哪几段（写清时段＋时区）？哪几段绝对不能被打断？**

四个人都还没报过。**没有这张时段表，就只能写「谁负责」，写不出「几点前能拿到」。**
它不影响前三条开工 —— 前端有 mock 了、崇铭有两件可独立做的、capo 有构建链 —— 但它决定第 4 角色（却白的「持续测试、端到端可用」）能不能写成时间承诺。

---

*本文档的证据等级：只改文档 + 新增 `evidence/mock/`。mock 一侧有验收脚本（59/59 实测通过）；文中「主流程不在公网版」为**实测**（线上页面 0 处「本次尚未验收」，构建输入确为壳）；「形态此前未写进文件」为**全库 grep 实测**（`多邻国` 仅出现在一份 Duolingo 公司调研里，非形态定义）。*
