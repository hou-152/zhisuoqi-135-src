# 工作日志 · 知所栖 135

**唯一落点**。逐轮记录「做了什么、凭什么说做完了、还没做什么」。
`SOURCE_OF_TRUTH.md` 只保留指向本文件的入口，不逐条登记本文件里的变更 —— 那正是导航膨胀的来源。

读法：找**结论**去 `SOURCE_OF_TRUTH.md`；找**这一轮干了什么、验过没有**读这里；找**某个东西是什么**读每节末尾的落点文件。

---

## 2026-09-12 · 第二轮（08:0x – 10:2x）：把「摆设」改成「倒逼」，再把它送上公网和桌面

这一轮全部由所有者的当场批评驱动。**六次批评，六次返工**，每一次的原始原话都记在下面 ——
因为这一轮真正值钱的不是做了什么，是**被什么逼出来的**。

### ① 策展层 —— 「265 条待你判」是把活外包

> 「你有没有去全量铺开嘛？为什么还要我去判断呢？你不说把这些标签全部都弄上，
> 但你至少把一些策展给弄上吧，对不对？」

**之前**：概念图横轴写死 12 个来源分列；左栏写着「**265 条待你判**」。
把 265 次判断推回给用户，这不是策展。

**做了什么**
- `scripts/curate-concepts.mjs` → `evidence/concept-curation-20260912.json`
  194/194 全部打主题标签、**0 条未分类**；10 条主题线各带「为什么值得走」+ 入口（组内没有前置且解锁最多）+ 沿**真实依赖边**的路线 + 收敛点。
- `scripts/curate-edges.mjs` → `evidence/concept-edges-curated-20260912.json`
  266 条候选边逐条预判：**机器落地 250 条（采纳 169 / 跳过 81），只剩 16 条要人看**。

**踩过的坑（重要）**：留人的口径是「**模型自己说 unsure**」，不是「把握低于某阈值」。
先用 `conf ≥ 0.7` 当阈值，157 条模型其实判过的边被算成「没判」——**比原来更糟**。

**没做的**：词表覆盖不均（4 vs 41，没回去硬掰）；250 条是单模型跑一次、**无第二来源复核**；未铺开到 86 篇命中文章。
**落点**：`docs/策展层-194点10条线-20260912.md`

### ② 倒逼层 —— 概念图是个摆设

> 「变成了一个摆设，这是第一个点。第二点，概念其实也就那样吧。
> 这样子干你没法倒逼，当这种教学学习啊」

**摆设的源代码就一行**：

```js
function setMark(id, v) { marks[id] = v; saveMarks(); }   // 点一下「学过」就变绿，零证据
```

**做了什么**
- 删掉自报通道。概念面板改成「遮住上面那句，用自己的话说一遍」→ LLM 判定 →
  给 `missing` / `wrong` / **`backTo`（漏哪倒回哪）**，`backTo` 只能指真实先修概念，渲染成按钮。
- 判定口径写死在提示词里：**照抄不算过、只写几个词不算过、「意思差不多」不放过、拿不准就 fail**。
- 三色环：青=复述过了 / 红=没过·倒回中 / **琥珀=只过机械检查（明确标注不算学会）**。
- 策展面板每条线加「开始倒逼 · 一步一个概念」，过了才给下一个。

**「概念其实也就那样」的正面回答**：同意。所以没有把概念做得更漂亮，而是**把概念清单从产品降成原料** ——
概念不值钱，被逼着说出来、被指出漏在哪、被打回去重来，才值钱。

**落点**：`docs/倒逼层-概念图不是摆设-20260912.md`

### ③ 分类层 —— 「概念分类一般吧」

> 「概念分类一般吧，我觉得」

原 10 条主题线是**主题聚类** —— 任何模型喂 194 条小标题都能吐一批，且不告诉你拿它干什么。
**更要紧的是它暴露了倒逼层的一个真 bug**：194 个概念用**同一个验收标准**（说清核心机制），
对「成本三因子公式」合理，对「第二次文艺复兴」是**假考**。

**做了什么**：`scripts/classify-concepts.mjs` → `evidence/concept-classes-20260912.json`，
按**验收方式**四分（194/194）：

| 类 | 条数 | 过的方式 |
|---|---:|---|
| 能算的 | 69 | 复述核心机制 |
| 能判的 | 57 | 写出**你自己的判据和代价** |
| 能用的 | 27 | 给一个**你真拿它做过的具体例子** |
| 只能认的 | 41 | **不设验收** —— 硬考就是假学习 |

分类**决定系统对你做什么**：任务词、判定口径、有没有交卷按钮，全按类走。
公网无模型时 `能判的`/`能用的` **直接按没过记**（机械检查看不见「你的判据」和「你的例子」）。

**落点**：`docs/分类层-要你怎么处理它-20260912.md`

### ④ 外壳 —— 「没有 linear 的味道」

> 所有者贴出真 Linear 的 Inbox 截图：「没有 linear 的味道」

**对的，而且不是配色问题，是结构问题。** 之前是「全屏画布 + 滑出抽屉」——
**Linear 的骨架是三栏，中间那一栏（可扫列表）根本没有**。

**做了什么**：导航 224（5 项各带计数 + 分组行）/ 列表 302（一行一个东西，行高 30）/
主区画布 + 工具行 / 详情 412 **停靠让位不盖图**。配色收到 `#08090a`/`#1b1c1e`/`#5e6ad2`，去光晕去渐变。

**顺带修三个 bug**：`.dot` 是 inline 导致底部色点不显示；`resize()` 没减掉 412 导致详情打开时右两列被切；
左栏 `e2`/`e5` 都写成 `tags.length` 导致两个数字同为 10。

**落点**：`docs/外壳-Linear三栏-20260912.md`

### ⑤ 公网版 —— 「你要有在公网啊」

> 「兄弟，你要有在公网啊，你懂吗？要在公网上面能给人看到」

**这是赛制必交件**（`docs/知乎黑客松-赛制要求与项目差距.md` 早就写着「135 未上公网」）。

**做了什么**：`scripts/build-public.mjs` 把壳烘成**单文件、零依赖、零服务端**的页面。
- 线上：**<https://hou-152.github.io/>** → 跳 <https://hou-152.github.io/zhisuoqi-135/>
- 离线单文件：<https://github.com/hou-152/zhisuoqi-135/raw/main/index.html>（双击就开，不联网）
- 对话三条路：本地服务端 → **访客自带 DeepSeek key**（只进他自己 localStorage）→ **录制回放**（明说不是本次回答）
- 根站 `deploy/root-site/`：GitHub Pages 规则是「没有 `<用户名>.github.io` 仓库时裸域名根路径 404」，
  所有者打开过一次 404，建根站堵掉。

**走了弯路**：我在消息里把 `zhisuoqi.sibohou792.chatgpt.site/135.html` **说成了可用地址**，
它当时还是 404（要 Codex push 才存在）。所有者打开 404，已更正并记进证据。

**国内可达性（如实）**：阿里 / DNSPod / 360 三大国内 DNS 都返回 GitHub Pages 真 IP
（185.199.108-111.153）→ **DNS 未被污染**。但**无法从墙内直连验证 TCP**，本机走 fake-IP 代理。

### ⑥ 桌面版 —— 「为什么不变成 app」

> 「为什么不变成 app」

**因为我把「必交件是链接」当成了「所以不做 app」——这是偷懒。** 所有者最早的原话是
「先做 APP 吧，如果可以的话；不行的话，再去做一个网站」。

**这一版不是换个壳**：网页版有两件事根本做不到，其中一件**正是我自己写进文档、标成「能力边界」的那条**：

| | 网页版 | 桌面版 |
|---|---|---|
| 学习记录 | localStorage，清一次浏览器数据就没了 | `~/Documents/知所栖-135/学习记录.json` |
| skill 产物 | `dbs-learning-beta` 的 SKILL.md **写死了**产物路径，网页版只能回「当前会话没有文件写入能力，先直接贴出」 | **真写文件**，那条路径第一次被兑现 |

**做法**：不重写第二份服务 —— `scripts/serve-lib.mjs` 是唯一一份，命令行壳（5180）与 Electron 壳（随机端口）都 import 它。

**打包踩的坑**：① `main.js` 原来 import `../scripts/serve-lib.mjs`，而 packager 只装 `app/` →
打出来的 `.app` **起不来且零报错**；② 不能把项目根当 source（`.private/` 会进可分发物）→
构建时加**凭证自检**，扫到 `llm.env` / `access-secret` / `*.key` 直接失败。打包版 `/api/health` 回 `llm:false`，**这是对的**。

**顺带修一个真缺陷**：`/api/save` 原来把路径里的 `..` **过滤掉**而不是拒绝 ——
`../../../../tmp/evil.md` 会被静默改写成 `DATA_DIR/tmp/evil.md` 写下去。它没跳出目录，
但**把一次越界尝试变成了一次成功的写入**。现在直接拒绝 + `join` 后再校验。

**落点**：`docs/桌面版-为什么要有它-20260912.md`

---

## 这一轮的验收脚本总表

| 脚本 | 验什么 | 条数 | 前置 | 上次实测 |
|---|---|---:|---|---|
| `scripts/verify-135.mjs` | 主产物 `知所栖-135-基础框架.html` 全流程 | **61** | serve 在跑 | 61/61 ✅ |
| `scripts/test-daobi.mjs` | 倒逼层 + 分类层 + 三栏外壳 | **24** | serve 在跑（真 LLM） | 全过 ✅ 连跑两次稳定 |
| `scripts/check-public.mjs` | 公网版（**必须假域名 + `--host-resolver-rules`**，用 127.0.0.1 会走服务端分支，测了个寂寞） | 17 | 静态服务或线上地址 | 全过 ✅ |
| `scripts/shot-shell.mjs` | 壳的截图 + 面板越界断言 | 11 步 | serve 在跑 | 0 JS 报错 ✅ |
| `scripts/test-app.mjs` | 桌面版：真启动 Electron + CDP，含路径越界必须拒绝 | **12** | `app/node_modules` | 全过 ✅ |
| `scripts/ab-feynman-test.mjs` | AB 实验（A 免 key；B/C1 需凭证；无 `ab-samples.json` 会拒跑） | — | `.private/llm.env` | — |

**判定器不是确定性的**：`deepseek-flash` 是推理模型，`temperature=0` 也不保证逐字复现，
边界答案会 pass/fail 翻转。所以 `test-daobi.mjs` 里的「好答案」是**在真壳里验到 `missing:[] wrong:[]` 才固化**的，
通过侧断言带一次重试并打印判定器给的 `why`。

---

## 一条值得单独记的事：判定器在我自己写的测试答案里挑出 4 处真错

| # | 我写的 | 判定器指出 |
|---|---|---|
| 1 | 「外在·集体是**右上角**」 | 四象限里上面是个体、下面是集体 → 应是**右下角** |
| 2 | 「砍掉了外在·集体那一**整行**」 | 行列混用，那是一个**格** |
| 3 | 「要补**内在**那一列的空缺」→ 却填了**外在·集体** | 前后自相矛盾 |
| 4 | 拿四象限算自己的时间账 | C04 的定义是「**定位纯粹派与自动机各砍掉哪半个现实**」，根本没落到那两个人身上 |

**四次都是判定器对、我错。** 这是「倒逼层真的在挑刺」最硬的证据 —— 不是我说的，是实测记录。

---

## 未决事项（照实标，不要假装已解决）

| # | 事项 | 状态 |
|---|---|---|
| 1 | `.agents/skills/` 与 `app/assets/.agents/skills/` 里 **15 个 dbs skill 原文的再分发范围** | **未决**。公网版烘了 2 个（`dbs-learning-beta`、`dbs-standard-answer`），桌面版烘了 14 个。`dbs-*` 来自 dontbesilent 的 dbskill，若原作者要求移除，删对应目录即可 |
| 2 | `zhisuoqi.sibohou792.chatgpt.site/135.html` | **仍是 404**。要 Codex commit + push 才存在（我推不上去，远端要 Codex 环境的凭证） |
| 3 | 项目主仓库 | **未推 GitHub**。`.private/` `.local/` `.mimosa/` `.trash/` `.playwright-cli/` `deploy/*/` `app/node_modules/` `app/dist/` `app/assets/` 均已 gitignore |
| 4 | 桌面版 | 只打了 **macOS arm64**；**未签名未公证**，别人打开会被 Gatekeeper 拦。306MB 解包 / 122MB zip |
| 5 | 策展的边预判 | 250 条是**单模型跑一次**，无第二来源复核。置信度是模型自报的，不是实测的 |
| 6 | 分类 | 模型单跑一次，我只手工抽查 9 条，**未做全量人工审计**；界面上**无法改判**（要改 JSON 重跑构建） |
| 7 | 「只能认的不设验收」 | 是**编辑判断不是研究结论**。不得对外声称「不考世界观更有效」 |
| 8 | 概念语料 | 194 条只来自 **12 篇**（当时逐篇读 markdown 的额度限制），命中 86 篇，**未全量铺开** |
| 9 | 人气奖 / 知乎 OAuth | 已裁决**放弃**；OAuth 与公网部署是同一件事，公网已上，OAuth 仍未接（非必做） |

---

## 已知的技术债

| 位置 | 问题 |
|---|---|
| `scripts/shell.template.html.bak` / `.bak2` | 本轮打补丁留下的临时备份（38KB / 49KB），**该清掉** |
| 壳的列表栏 | 194 行一次渲染，**无虚拟滚动**；上千行会卡 |
| 壳的键盘 | **无 `j/k`、无 `⌘K`、无 `Esc`** |
| 窄屏 <1100px | 列表栏**直接隐藏**，是权宜不是设计过的降级 |
| 桌面版 | **无自动更新**；无 preload/IPC（有意：窗口走本地 HTTP，`contextIsolation` 开着、`nodeIntegration` 关着） |

---

## 怎么接着干（给下一个接手方）

```sh
# 起服务（命令行版）
node scripts/serve-135.mjs              # → http://127.0.0.1:5180/知所栖-壳.html
# 起桌面版（能写真文件）
cd app && npm start
# 重新生成一切
node scripts/curate-concepts.mjs && node scripts/curate-edges.mjs   # 有缓存，改判据不重烧 token
node scripts/build-shell.mjs            # → prototype/知所栖-壳.html
node scripts/build-public.mjs           # → deploy/zhisuoqi-135/index.html
node scripts/build-app.mjs              # → app/dist/知所栖 135.app
# 验收（全部要有 serve 在跑，test-app 除外）
node scripts/verify-135.mjs && node scripts/test-daobi.mjs
node scripts/check-public.mjs && node scripts/test-app.mjs
```

**铁律**（都有实测代价）：
1. 改了 `scripts/` 或 `prototype/` 就**跑一遍对应的验收脚本**，跑过再说话。
2. 无头 Chrome 抓不到 `transform` 过渡中的元素 —— **DOM 断言优先于肉眼读图**。
3. 测公网版**必须用假域名**（`--host-resolver-rules`），用 `127.0.0.1` 会走服务端那条路。
4. 判定/生成类的 LLM 调用**不要用固定 sleep**，轮询状态；通过侧断言要能重试。
5. 结论文件与代码冲突时，**以实测为准并报告冲突**，不擅自指定当前版本。
