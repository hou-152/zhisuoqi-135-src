# 这层放什么

**放**：发生过什么——客观事实、相关方笔记、稳定信息、所有者的长期偏好。

**不放**：AI 的判断（那是 02 或 04）、还没验证的假设（04）、某一时点的整体画像（03）。

**常见误放**：把「AI 建议这么做」写进来（那是 `[AI 判断]`，进 04）；把一次性的临时状态写进来（进 `我的当前状态.md`）。

规则：**只追加，不改写已有条目。** 情况变了就追加，不覆盖。
