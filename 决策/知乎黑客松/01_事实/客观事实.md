# 客观事实

只追加。每条标来源与日期。

## 项目与环境（2026-09-12 核）

- `[文件记载]` 项目根 `/Users/housibo/Documents/知乎黑客松`。git 已初始化，**6 个提交**（`git rev-list --count HEAD` 实测），未推 GitHub。
- `[文件记载]` 本机 Node v24.15.0、pnpm 11.7.0；**无 Rust/cargo**（Tauri 官方要求 Rust，故 48h 内不成立）。
- `[文件记载]` 本机 Chrome 152、Safari；`playwright-cli` 在 `~/.npm/_npx/31e32ef8478fbf80/`，**禁 file:// 协议**。
- `[文件记载]` `verify-135.mjs` 含 `check(` **62 行，其中 1 行是函数定义（L58）→ 实际调用 61 次**。61 是对的。
- `[文件记载]` `SOURCE_OF_TRUTH.md` 中 62 条反引号路径，59 条真实存在；3 条「缺失」均为假阳性（`/api/health` 是接口、`/tmp/HANDOFF-*.md` 是故意作废路径、`evals/` 相对 skill 目录）。**路径层无失效。**
- `[文件记载]` `research/学习记录-存储与所有权-16产品调研-20260912.md` 实际有 **59 条**编号来源。

## 成本（harness 台账实测，2026-09-12）

- `[文件记载]` 本机 `~/.dsh/storages/cost-meter/ledger.json`。当日 2,714 次调用，进上下文 419,723,720 tokens，输出 2,816,189，**¥4.16**；缓存命中率 98%；单价 缓存命中 ¥0.003 / 未命中 ¥0.15 / 输出 ¥0.60（每百万 token）。
- `[文件记载]` Agent 每次调用平均进上下文 **154,651 tokens**；固定脚本单步 **1,074 tokens**（`docs/知乎API-实测-260912.md` §3.4）→ 差 **144 倍**。
- `[AI 判断]` 该差额被缓存吸收，故人民币成本很低；**token 量与行为是两套账**。

## 交互式学习的事实（原型代码实测）

- `[文件记载]` `prototype/知所栖-135-基础框架.html` L920 原文：「下一步：**换一个新问题进入下一轮**（验收结果作为下一轮的起点）。」→ **该产品在一次会话内存在「下一轮」。**
- `[文件记载]` 同文件 L392 `pass: hits >= 2`（三个要点答对两个即判 pass）；LLM 失败静默回退到 `ruleBasedReview`。
- `[文件记载]` 同文件 L916 倒回按钮只在 `d.hit` 为假时渲染 → 判过 pass 的概念永不回头。
- `[文件记载]` 同文件 L173 是 `saveCheckpoint()` **唯一调用点**（手动按钮）；L312 每次加载都是 `freshState()` → 跨页面加载不是默认，是可选的。
- `[文件记载]` `remediation` 全文件仅出现在 L314/315/905/909，**无一处界面渲染**。

## 本次会话的代码改动（2026-09-12）

- `[文件记载]` 改了 `scripts/concept-map.template.html`（加标记层：学过青环 / 漏点红环 / 清除三按钮，落 `localStorage` key `zss135.canvas.v1`）；重建产物 `prototype/知所栖-135-概念图.html`（194 点 / 353 边，48→64 KB）。
- `[文件记载]` 改前产物备份：`.trash/2026-09-12_概念图_加标记层前.html`（61,218 字节）。
- `[文件记载]` 无头验证通过：标记前后坐标 `[370.799, 206.709]` 完全一致（**位置不动**）、落盘、刷新存活、图例计数实时、194/353 数据未变。
- `[文件记载]` **未验证**：`file://` 双击打开时 `localStorage` 能否写入（playwright 禁 file 协议）。
