# 这层放什么

**放**：看出什么——从多次事实里炼出的概念和模式。

**进这层的门槛**（三条里至少满足两条）：① 在 3 次以上事实里出现过；② 能解释多条 `01_事实/` 条目；③ 对下一步有明确指导作用。

**不放**：只出现一两次的观察（先用 `[AI 暂定概念 YYYY-MM-DD]` 写在 04）、一次性的结论。

规则：**缓慢追加修正。原文不重写**，用 `[修正 YYYY-MM-DD]` 在原段下方追加。
