# 待验证假设

| 假设 | 当前状态 | 怎么验 |
|---|---|---|
| `file://` 双击打开时 `localStorage` 能写 | **未核实**（playwright 禁 file 协议） | 双击 `prototype/知所栖-135-概念图.html` → 点一个点 → 标「学过」→ 刷新 → 环还在＝能写 |
| 四轮聊天室给的「先修检错器再通电」能提升演示说服力 | 未验证 | 拿一条已知漏了某要点的复述跑一遍，看是否被正确判为有漏点并倒回 |
| 一次会话内能完成一次「合成」（Sweller 的 composition） | 未验证（专家自己说没有数据） | 数每轮新产生的条件—动作对，看第几轮不再新增 |
| 「一轮追问」能带来 ICAP 的 C→I 台阶 | 未验证 | 对比有无追问两种情况下，第 3 节起点是否 ≠ 第 1 节起点 |
| 画布标记能替代部分对话（手工标记次数会因自动判定而下降） | 未验证 | 接上自动判定后，统计手工标记占比 |

## 已作废（留档，不要再引）

- ~~该产品在一次会话内没有「下一轮」~~ —— `[AI 判断]` 判官说的，`[本人]` 当场推翻，核实后**用户是对的**：`prototype/知所栖-135-基础框架.html` **L920** 原文写着「换一个新问题进入下一轮」。
